# DISABLED — Vercel handles deployment automatically via GitHub integration.
# Vercel watches the main branch and deploys on every push.
# This file is kept for reference only.
#
# To re-enable GitHub Pages deployment in the future, uncomment the content
# below and remove the Vercel integration in the Vercel dashboard.

# name: Deploy to GitHub Pages
# on:
#   push:
#     branches: [ main ]
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#       - run: npm install
#       - run: npm run build
#       - uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./dist
#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     permissions:
#       pages: write
#       id-token: write
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - uses: actions/deploy-pages@v4
#         id: deployment
