import { c as createComponent } from './astro-component_CdpYp1nz.mjs';
import 'piccolore';
import { L as renderTemplate, b7 as defineScriptVars, x as maybeRenderHead, a2 as addAttribute, F as Fragment } from './sequence_B8w407xz.mjs';
import { r as renderComponent } from './entrypoint_Diq9N73R.mjs';
import { $ as $$AdminLayout } from './AdminLayout_DI2FboVo.mjs';
import { adminDb } from './firebase-admin_xK7nQJo9.mjs';
import { FieldValue } from 'firebase-admin/firestore';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Planning = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$props, $$slots);
  Astro2.self = $$Planning;
  const user = Astro2.locals.user;
  const firebaseConfig = {
    apiKey: "AIzaSyAP3QgOUeZ1ruwoUxOROvKw1JXpTxYAIQg",
    authDomain: "magnaarts.firebaseapp.com",
    projectId: "magnaarts",
    storageBucket: "magnaarts.firebasestorage.app",
    messagingSenderId: "1084773655627",
    appId: "1:1084773655627:web:3501bdbb5fbfb04952d9ce"
  };
  const [seasonsSnap, shellsSnap, openMicSnap, artNightSnap, festivalSnap, approvedBandsSnap, vendorSnap, artClassSnap] = await Promise.all([
    adminDb.collection("park_seasons").orderBy("year", "desc").get(),
    adminDb.collection("park_events").orderBy("date", "asc").get(),
    adminDb.collection("open_mic_events").orderBy("date", "asc").get(),
    adminDb.collection("art_night_events").orderBy("date", "asc").get(),
    adminDb.collection("festival_events").orderBy("date", "asc").get(),
    adminDb.collection("band_applications").where("status", "in", ["approved", "waitlisted"]).orderBy("band_name", "asc").get(),
    adminDb.collection("vendor_applications").where("status", "in", ["approved", "paid"]).get(),
    adminDb.collection("art_class_submissions").where("status", "in", ["pending", "approved", "contacted"]).orderBy("submittedAt", "desc").get()
  ]);
  const allShells = shellsSnap.docs.map((d) => ({ id: d.id, ...d.data() }));
  const allOpenMicShells = openMicSnap.docs.map((d) => ({ id: d.id, ...d.data() }));
  const allArtNightShells = artNightSnap.docs.map((d) => ({ id: d.id, ...d.data() }));
  const allFestivalShells = festivalSnap.docs.map((d) => ({ id: d.id, ...d.data() }));
  const approvedBands = approvedBandsSnap.docs.map((d) => ({ id: d.id, ...d.data() }));
  const approvedVendorCount = vendorSnap.size;
  const artClassSubmissions = artClassSnap.docs.map((d) => ({ id: d.id, ...d.data() }));
  let flashMsg = "";
  let flashType = "";
  if (Astro2.request.method === "POST") {
    const form = await Astro2.request.formData();
    const action = form.get("action")?.toString();
    if (action === "create_season") {
      const year = parseInt(form.get("year")?.toString() || "0");
      const venue = form.get("venue")?.toString() || "";
      const address = form.get("address")?.toString() || "";
      const start = form.get("startTime")?.toString() || "7:00 PM";
      const datesRaw = form.get("dates")?.toString() || "";
      const seriesType = form.get("seriesType")?.toString() || "music_in_park";
      const dates = datesRaw.split("\n").map((d) => d.trim()).filter((d) => /^\d{4}-\d{2}-\d{2}$/.test(d));
      if (!year || dates.length === 0) {
        flashMsg = "Please enter a valid year and at least one date.";
        flashType = "error";
      } else if (seriesType === "open_mic") {
        const omTitle = form.get("omTitle")?.toString() || "Open Mic Night";
        const omImageUrl = form.get("omImageUrl")?.toString() || "";
        const omDesc = form.get("omDescription")?.toString() || "";
        const existing = await adminDb.collection("open_mic_events").where("season", "==", year).get();
        const existingDates = new Set(existing.docs.map((d) => d.data().date));
        const batch = adminDb.batch();
        let created = 0;
        for (const date of dates) {
          if (existingDates.has(date)) continue;
          const ref = adminDb.collection("open_mic_events").doc();
          const d = /* @__PURE__ */ new Date(date + "T12:00:00");
          const display = d.toLocaleDateString("en-US", { weekday: "long", year: "numeric", month: "long", day: "numeric" });
          batch.set(ref, {
            season: year,
            date,
            displayDate: display,
            startTime: start,
            venue,
            address,
            title: omTitle,
            imageUrl: omImageUrl,
            description: omDesc,
            eventType: "open_mic",
            status: "shell",
            createdAt: FieldValue.serverTimestamp()
          });
          created++;
        }
        await batch.commit();
        flashMsg = `Open Mic Night: ${created} new shell(s) created (${dates.length} dates total).`;
        flashType = "success";
      } else if (seriesType === "group_art_night") {
        const anTitle = form.get("anTitle")?.toString() || "Group Art Night";
        const anImageUrl = form.get("anImageUrl")?.toString() || "";
        const anDesc = form.get("anDescription")?.toString() || "";
        const existing = await adminDb.collection("art_night_events").where("season", "==", year).get();
        const existingDates = new Set(existing.docs.map((d) => d.data().date));
        const batch = adminDb.batch();
        let created = 0;
        for (const date of dates) {
          if (existingDates.has(date)) continue;
          const ref = adminDb.collection("art_night_events").doc();
          const d = /* @__PURE__ */ new Date(date + "T12:00:00");
          const display = d.toLocaleDateString("en-US", { weekday: "long", year: "numeric", month: "long", day: "numeric" });
          batch.set(ref, {
            season: year,
            date,
            displayDate: display,
            startTime: start,
            venue,
            address,
            title: anTitle,
            description: anDesc,
            imageUrl: anImageUrl,
            eventType: "group_art_night",
            status: "shell",
            presenterId: null,
            presenterName: null,
            presenterConfirmed: false,
            createdAt: FieldValue.serverTimestamp()
          });
          created++;
        }
        await batch.commit();
        flashMsg = `Group Art Night: ${created} new shell(s) created (${dates.length} dates total).`;
        flashType = "success";
      } else if (seriesType === "arts_festival") {
        const afTitle = form.get("afTitle")?.toString() || "Magna Main Street Arts Festival";
        const afDesc = form.get("afDescription")?.toString() || "";
        const afStartTime = form.get("afStartTime")?.toString() || start;
        const afEndTime = form.get("afEndTime")?.toString() || "";
        if (dates.length !== 1) {
          flashMsg = "Please select exactly one date for the Arts Festival.";
          flashType = "error";
        } else {
          const date = dates[0];
          const d = /* @__PURE__ */ new Date(date + "T12:00:00");
          const display = d.toLocaleDateString("en-US", { weekday: "long", year: "numeric", month: "long", day: "numeric" });
          const festYear = d.getFullYear();
          const existing = await adminDb.collection("festival_events").where("season", "==", festYear).get();
          if (!existing.empty) {
            flashMsg = `A festival shell for ${festYear} already exists. Delete it first to recreate.`;
            flashType = "error";
          } else {
            const ref = adminDb.collection("festival_events").doc(`arts-festival-${festYear}`);
            await ref.set({
              season: festYear,
              date,
              displayDate: display,
              startTime: afStartTime,
              endTime: afEndTime,
              venue,
              address,
              title: afTitle,
              description: afDesc,
              eventType: "arts_festival",
              status: "shell",
              image: "/images/festival/mmsaf.png",
              createdAt: FieldValue.serverTimestamp()
            });
            flashMsg = `Arts Festival shell created for ${display}.`;
            flashType = "success";
          }
        }
      } else {
        await adminDb.collection("park_seasons").doc(String(year)).set({
          year,
          venue,
          address,
          startTime: start,
          dateCount: dates.length,
          createdAt: FieldValue.serverTimestamp(),
          createdBy: user?.email || "admin"
        });
        const existing = await adminDb.collection("park_events").where("season", "==", year).get();
        const existingDates = new Set(existing.docs.map((d) => d.data().date));
        const batch = adminDb.batch();
        let created = 0;
        for (const date of dates) {
          if (existingDates.has(date)) continue;
          const ref = adminDb.collection("park_events").doc();
          const d = /* @__PURE__ */ new Date(date + "T12:00:00");
          const display = d.toLocaleDateString("en-US", { weekday: "long", year: "numeric", month: "long", day: "numeric" });
          batch.set(ref, {
            season: year,
            date,
            displayDate: display,
            startTime: start,
            venue,
            address,
            eventType: "music_movie_in_park",
            status: "shell",
            bandId: null,
            bandName: null,
            bandConfirmed: false,
            movie: null,
            movieToken: null,
            createdAt: FieldValue.serverTimestamp()
          });
          created++;
        }
        await batch.commit();
        flashMsg = `Music & Movies in the Park: Season ${year} set up. ${created} new shell(s) created.`;
        flashType = "success";
      }
    }
    if (action === "delete_shell") {
      const shellId = form.get("shellId")?.toString();
      const col = form.get("collection")?.toString() || "park_events";
      if (shellId) {
        await adminDb.collection(col).doc(shellId).delete();
        flashMsg = "Event shell deleted.";
        flashType = "success";
      }
    }
    if (action === "assign_festival_band") {
      const shellId = form.get("shellId")?.toString();
      const slot = parseInt(form.get("slot")?.toString() || "0");
      const bandId = form.get("bandId")?.toString();
      const bandName = form.get("bandName")?.toString();
      const slotTime = form.get("slotTime")?.toString() || "";
      if (shellId && slot && bandId) {
        const shellRef = adminDb.collection("festival_events").doc(shellId);
        const shellSnap = await shellRef.get();
        const shellData = shellSnap.data();
        const bands = shellData?.bands || [
          { slot: 1, time: "", bandId: null, bandName: null, confirmed: false },
          { slot: 2, time: "", bandId: null, bandName: null, confirmed: false },
          { slot: 3, time: "", bandId: null, bandName: null, confirmed: false }
        ];
        const idx = bands.findIndex((b) => b.slot === slot);
        if (idx >= 0) {
          bands[idx] = { slot, time: slotTime, bandId, bandName, confirmed: false };
        } else {
          bands.push({ slot, time: slotTime, bandId, bandName, confirmed: false });
        }
        const batch = adminDb.batch();
        batch.update(shellRef, { bands, status: "planning" });
        batch.update(adminDb.collection("band_applications").doc(bandId), {
          status: "assigned",
          assignedShellId: shellId,
          assignedAt: FieldValue.serverTimestamp()
        });
        await batch.commit();
        flashMsg = `${bandName} assigned to Stage Slot ${slot}.`;
        flashType = "success";
      }
    }
    if (action === "unassign_festival_band") {
      const shellId = form.get("shellId")?.toString();
      const slot = parseInt(form.get("slot")?.toString() || "0");
      if (shellId && slot) {
        const shellRef = adminDb.collection("festival_events").doc(shellId);
        const shellSnap = await shellRef.get();
        const shellData = shellSnap.data();
        const bands = (shellData?.bands || []).map(
          (b) => b.slot === slot ? { slot, time: "", bandId: null, bandName: null, confirmed: false } : b
        );
        const allEmpty = bands.every((b) => !b.bandId);
        const removedBand = (shellData?.bands || []).find((b) => b.slot === slot);
        const batch = adminDb.batch();
        batch.update(shellRef, { bands, status: allEmpty ? "shell" : "planning" });
        if (removedBand?.bandId) {
          batch.update(adminDb.collection("band_applications").doc(removedBand.bandId), {
            status: "approved",
            assignedShellId: null,
            assignedAt: null
          });
        }
        await batch.commit();
        flashMsg = `Slot ${slot} cleared.`;
        flashType = "success";
      }
    }
    if (action === "confirm_festival_band") {
      const shellId = form.get("shellId")?.toString();
      const slot = parseInt(form.get("slot")?.toString() || "0");
      if (shellId && slot) {
        const shellRef = adminDb.collection("festival_events").doc(shellId);
        const shellSnap = await shellRef.get();
        const bands = (shellSnap.data()?.bands || []).map(
          (b) => b.slot === slot ? { ...b, confirmed: true } : b
        );
        const allConfirmed = bands.filter((b) => b.bandId).every((b) => b.confirmed);
        await shellRef.update({ bands, status: allConfirmed ? "confirmed" : "planning" });
        flashMsg = `Slot ${slot} confirmed.`;
        flashType = "success";
      }
    }
    if (action === "assign_presenter") {
      const shellId = form.get("shellId")?.toString();
      const presenterId = form.get("presenterId")?.toString();
      const presenterName = form.get("presenterName")?.toString();
      if (shellId && presenterId) {
        await adminDb.collection("art_night_events").doc(shellId).update({
          presenterId,
          presenterName,
          presenterConfirmed: false,
          status: "presenter_assigned"
        });
        flashMsg = `${presenterName} assigned as presenter.`;
        flashType = "success";
      }
    }
    if (action === "unassign_presenter") {
      const shellId = form.get("shellId")?.toString();
      if (shellId) {
        await adminDb.collection("art_night_events").doc(shellId).update({
          presenterId: null,
          presenterName: null,
          presenterConfirmed: false,
          status: "shell"
        });
        flashMsg = "Presenter removed.";
        flashType = "success";
      }
    }
    if (action === "confirm_presenter") {
      const shellId = form.get("shellId")?.toString();
      if (shellId) {
        await adminDb.collection("art_night_events").doc(shellId).update({
          presenterConfirmed: true,
          status: "confirmed"
        });
        flashMsg = "Presenter confirmed.";
        flashType = "success";
      }
    }
    if (action === "update_shell") {
      const shellId = form.get("shellId")?.toString();
      const venue = form.get("venue")?.toString();
      const address = form.get("address")?.toString();
      const startTime = form.get("startTime")?.toString();
      const col = form.get("collection")?.toString() || "park_events";
      const newImageUrl = form.get("imageUrl")?.toString() || null;
      const displayType = form.get("displayType")?.toString() || null;
      const movieTitle = form.get("movieTitle")?.toString() || null;
      const moviePreviewEmbed = form.get("moviePreviewEmbed")?.toString() || null;
      if (shellId) {
        const updateData = { venue, address, startTime };
        if (newImageUrl) updateData.imageUrl = newImageUrl;
        if (col === "park_events") {
          updateData.display_type = displayType || "music_only";
          updateData.movie_title = movieTitle || null;
          updateData.movie_preview_embed = moviePreviewEmbed || null;
        }
        await adminDb.collection(col).doc(shellId).update(updateData);
        flashMsg = "Event shell updated.";
        flashType = "success";
      }
    }
    return Astro2.redirect("/admin/planning");
  }
  const shellsBySeason = {};
  for (const s of allShells) {
    if (!shellsBySeason[s.season]) shellsBySeason[s.season] = [];
    shellsBySeason[s.season].push(s);
  }
  const openMicBySeason = {};
  for (const s of allOpenMicShells) {
    if (!openMicBySeason[s.season]) openMicBySeason[s.season] = [];
    openMicBySeason[s.season].push(s);
  }
  const artNightBySeason = {};
  for (const s of allArtNightShells) {
    if (!artNightBySeason[s.season]) artNightBySeason[s.season] = [];
    artNightBySeason[s.season].push(s);
  }
  const festivalBySeason = {};
  for (const s of allFestivalShells) {
    if (!festivalBySeason[s.season]) festivalBySeason[s.season] = [];
    festivalBySeason[s.season].push(s);
  }
  const totalShells = Object.keys(shellsBySeason).length + Object.keys(openMicBySeason).length + Object.keys(artNightBySeason).length + Object.keys(festivalBySeason).length;
  return renderTemplate(_a || (_a = __template(["", " <!-- Inject Firebase config, approved bands, and art class submissions --> <script>(function(){", "\n  window.__FIREBASE_CONFIG__      = firebaseConfig;\n  window.__APPROVED_BANDS__       = approvedBands;\n  window.__ART_CLASS_SUBMISSIONS__ = artClassSubmissions;\n})();</script> <script type=\"module\">\n  import { initializeApp, getApps, getApp } from 'https://www.gstatic.com/firebasejs/11.6.0/firebase-app.js';\n  import { getStorage, ref, uploadBytes, getDownloadURL } from 'https://www.gstatic.com/firebasejs/11.6.0/firebase-storage.js';\n\n  const app     = getApps().length ? getApp() : initializeApp(window.__FIREBASE_CONFIG__);\n  const storage = getStorage(app);\n\n  // ── Event type dropdown ─────────────────────────────────────────────────\n  const EVENT_PRESETS = {\n    music_in_park:   { venue: 'Pleasant Green Park',        address: '3250 S 8400 W, Magna, UT 84044' },\n    open_mic:        { venue: 'Magna Library',               address: '2675 S 8950 W, Magna, UT 84044' },\n    group_art_night: { venue: 'Magna Library',               address: '2675 S 8950 W, Magna, UT 84044' },\n    annual_seminar:  { venue: 'Magna Library',               address: '2675 S 8950 W, Magna, UT 84044' },\n    arts_festival:   { venue: 'Historic Magna Main Street',  address: '8900 W to 9180 W Magna Main St, Magna, UT 84044' },\n    custom:          { venue: '',                            address: '' },\n  };\n  const PLACEHOLDER_TYPES = new Set(['annual_seminar', 'custom']);\n\n  const typeSelect    = document.getElementById('event-type-select');\n  const mainFormBody  = document.getElementById('main-form-body');\n  const placeholderEl = document.getElementById('placeholder-notice');\n  const omFields      = document.getElementById('open-mic-fields');\n  const anFields      = document.getElementById('art-night-fields');\n  const afFields      = document.getElementById('arts-festival-fields');\n  const venueInput    = document.getElementById('venue');\n  const addrInput     = document.getElementById('address');\n\n  typeSelect.addEventListener('change', () => {\n    const val = typeSelect.value;\n\n    if (!val) {\n      mainFormBody.hidden  = true;\n      placeholderEl.hidden = true;\n      return;\n    }\n    if (PLACEHOLDER_TYPES.has(val)) {\n      mainFormBody.hidden  = true;\n      placeholderEl.hidden = false;\n      return;\n    }\n\n    mainFormBody.hidden  = false;\n    placeholderEl.hidden = true;\n\n    const preset = EVENT_PRESETS[val];\n    if (preset) {\n      venueInput.value = preset.venue;\n      addrInput.value  = preset.address;\n    }\n\n    omFields.style.display = val === 'open_mic'        ? 'flex' : 'none';\n    anFields.style.display = val === 'group_art_night' ? 'flex' : 'none';\n    afFields.style.display = val === 'arts_festival'   ? 'flex' : 'none';\n  });\n\n  // ── Calendar ────────────────────────────────────────────────────────────\n  const yearInput   = document.getElementById('year');\n  const calGrid     = document.getElementById('cal-grid');\n  const datesHidden = document.getElementById('dates-hidden');\n  const chipsWrap   = document.getElementById('selected-chips');\n  const chipsList   = document.getElementById('chips-list');\n  const clearBtn    = document.getElementById('clear-dates-btn');\n\n  const MONTH_NAMES = ['January','February','March','April','May','June',\n                       'July','August','September','October','November','December'];\n  const DAY_ABBRS   = ['Su','Mo','Tu','We','Th','Fr','Sa'];\n\n  let selectedDates = new Set();\n\n  function pad(n) { return String(n).padStart(2, '0'); }\n  function dateKey(y, m, d) { return `${y}-${pad(m+1)}-${pad(d)}`; }\n\n  function buildCalendar(year) {\n    calGrid.innerHTML = '';\n    for (let m = 0; m < 12; m++) {\n      const monthEl = document.createElement('div');\n      monthEl.className = 'cal-month';\n\n      const heading = document.createElement('div');\n      heading.className = 'cal-month-heading';\n      heading.textContent = MONTH_NAMES[m];\n      monthEl.appendChild(heading);\n\n      const dowRow = document.createElement('div');\n      dowRow.className = 'cal-dow-row';\n      dowRow.style.cssText = 'display:grid;grid-template-columns:repeat(7,1fr);gap:2px;margin-bottom:2px;';\n      DAY_ABBRS.forEach(ab => {\n        const c = document.createElement('div');\n        c.className = 'cal-dow';\n        c.textContent = ab;\n        dowRow.appendChild(c);\n      });\n      monthEl.appendChild(dowRow);\n\n      const daysEl = document.createElement('div');\n      daysEl.className = 'cal-days';\n      daysEl.style.cssText = 'display:grid;grid-template-columns:repeat(7,1fr);gap:2px;';\n\n      const firstDow   = new Date(year, m, 1).getDay();\n      const daysInMonth = new Date(year, m+1, 0).getDate();\n\n      for (let i = 0; i < firstDow; i++) {\n        const blank = document.createElement('div');\n        blank.className = 'cal-day cal-blank';\n        daysEl.appendChild(blank);\n      }\n\n      for (let d = 1; d <= daysInMonth; d++) {\n        const key   = dateKey(year, m, d);\n        const dayEl = document.createElement('div');\n        dayEl.className = 'cal-day';\n        dayEl.textContent = d;\n        dayEl.dataset.date = key;\n        if (selectedDates.has(key)) dayEl.classList.add('cal-selected');\n\n        dayEl.addEventListener('click', () => {\n          if (selectedDates.has(key)) {\n            selectedDates.delete(key);\n            dayEl.classList.remove('cal-selected');\n          } else {\n            selectedDates.add(key);\n            dayEl.classList.add('cal-selected');\n          }\n          syncDates();\n        });\n\n        daysEl.appendChild(dayEl);\n      }\n\n      monthEl.appendChild(daysEl);\n      calGrid.appendChild(monthEl);\n    }\n  }\n\n  function syncDates() {\n    const sorted = Array.from(selectedDates).sort();\n    datesHidden.value = sorted.join('\\n');\n\n    if (sorted.length === 0) {\n      chipsWrap.hidden = true;\n      chipsList.innerHTML = '';\n      return;\n    }\n\n    chipsWrap.hidden = false;\n    chipsList.innerHTML = '';\n    sorted.forEach(key => {\n      const [y, mo, d] = key.split('-').map(Number);\n      const label = new Date(y, mo-1, d).toLocaleDateString('en-US', { month: 'short', day: 'numeric' });\n      const chip = document.createElement('span');\n      chip.className = 'date-chip';\n      chip.innerHTML = `${label} <button type=\"button\" class=\"chip-remove\" data-date=\"${key}\" aria-label=\"Remove ${label}\">×</button>`;\n      chipsList.appendChild(chip);\n    });\n\n    chipsList.querySelectorAll('.chip-remove').forEach(btn => {\n      btn.addEventListener('click', () => {\n        const key = btn.dataset.date;\n        selectedDates.delete(key);\n        const dayEl = calGrid.querySelector(`[data-date=\"${key}\"]`);\n        if (dayEl) dayEl.classList.remove('cal-selected');\n        syncDates();\n      });\n    });\n  }\n\n  clearBtn.addEventListener('click', () => {\n    selectedDates.clear();\n    calGrid.querySelectorAll('.cal-selected').forEach(el => el.classList.remove('cal-selected'));\n    syncDates();\n  });\n\n  buildCalendar(parseInt(yearInput.value) || new Date().getFullYear());\n  yearInput.addEventListener('change', () => {\n    selectedDates.clear();\n    syncDates();\n    buildCalendar(parseInt(yearInput.value) || new Date().getFullYear());\n  });\n\n  // ── Art Night image upload ─────────────────────────────────────────────\n  const anImageZone    = document.getElementById('an-image-zone');\n  const anImageInput   = document.getElementById('an-image-input');\n  const anImagePreview = document.getElementById('an-image-preview');\n  const anImageUrlEl   = document.getElementById('anImageUrl');\n  const anUploadStatus = document.getElementById('an-upload-status');\n\n  anImageZone.addEventListener('dragover', e => { e.preventDefault(); anImageZone.classList.add('drag-over'); });\n  anImageZone.addEventListener('dragleave', () => anImageZone.classList.remove('drag-over'));\n  anImageZone.addEventListener('drop', e => {\n    e.preventDefault();\n    anImageZone.classList.remove('drag-over');\n    if (e.dataTransfer?.files?.length) {\n      const dt = new DataTransfer();\n      dt.items.add(e.dataTransfer.files[0]);\n      anImageInput.files = dt.files;\n      anImageInput.dispatchEvent(new Event('change'));\n    }\n  });\n\n  anImageInput.addEventListener('change', () => {\n    const file = anImageInput.files?.[0];\n    if (!file) { anImagePreview.hidden = true; return; }\n    const reader = new FileReader();\n    reader.onload = ev => {\n      anImagePreview.hidden = false;\n      anImagePreview.innerHTML = `<img src=\"${ev.target.result}\" alt=\"Preview\" /><span>${file.name} (${(file.size/1024/1024).toFixed(1)} MB)</span>`;\n    };\n    reader.readAsDataURL(file);\n    anImageUrlEl.value = '';\n  });\n\n  // ── Open Mic image upload ───────────────────────────────────────────────\n  const omImageZone    = document.getElementById('om-image-zone');\n  const omImageInput   = document.getElementById('om-image-input');\n  const omImagePreview = document.getElementById('om-image-preview');\n  const omImageUrlEl   = document.getElementById('omImageUrl');\n  const omUploadStatus = document.getElementById('om-upload-status');\n\n  omImageZone.addEventListener('dragover', e => { e.preventDefault(); omImageZone.classList.add('drag-over'); });\n  omImageZone.addEventListener('dragleave', () => omImageZone.classList.remove('drag-over'));\n  omImageZone.addEventListener('drop', e => {\n    e.preventDefault();\n    omImageZone.classList.remove('drag-over');\n    if (e.dataTransfer?.files?.length) {\n      const dt = new DataTransfer();\n      dt.items.add(e.dataTransfer.files[0]);\n      omImageInput.files = dt.files;\n      omImageInput.dispatchEvent(new Event('change'));\n    }\n  });\n\n  omImageInput.addEventListener('change', () => {\n    const file = omImageInput.files?.[0];\n    if (!file) { omImagePreview.hidden = true; return; }\n    const reader = new FileReader();\n    reader.onload = ev => {\n      omImagePreview.hidden = false;\n      omImagePreview.innerHTML = `<img src=\"${ev.target.result}\" alt=\"Preview\" /><span>${file.name} (${(file.size/1024/1024).toFixed(1)} MB)</span>`;\n    };\n    reader.readAsDataURL(file);\n    omImageUrlEl.value = ''; // clear previously uploaded URL when new file chosen\n  });\n\n  // ── Form submit — upload image first if needed, then POST ───────────────\n  const seasonForm   = document.getElementById('season-form');\n  const submitBtn    = document.getElementById('submit-btn');\n  const submitStatus = document.getElementById('submit-status');\n\n  seasonForm.addEventListener('submit', async (e) => {\n    const isOpenMic = typeSelect.value === 'open_mic';\n    const file      = omImageInput.files?.[0];\n\n    // Validate dates for all active event types\n    if (mainFormBody && !mainFormBody.hidden && !datesHidden.value.trim()) {\n      e.preventDefault();\n      alert('Please select at least one date from the calendar.');\n      return;\n    }\n\n    const isMMP      = typeSelect.value === 'music_in_park';\n    const mmpFile    = document.getElementById('mmp-image-input')?.files?.[0];\n    const mmpUrlEl   = document.getElementById('mmpImageUrl');\n\n    if (isMMP && mmpFile && !mmpUrlEl?.value) {\n      e.preventDefault();\n      submitBtn.disabled = true;\n      submitStatus.hidden = false;\n      submitStatus.textContent = 'Uploading image…';\n      try {\n        const year = yearInput.value || '2026';\n        const ext  = mmpFile.name.split('.').pop();\n        const path = `mmpark-images/${year}/event-image.${ext}`;\n        const storageRef = ref(storage, path);\n        await uploadBytes(storageRef, mmpFile);\n        const url = await getDownloadURL(storageRef);\n        mmpUrlEl.value = url;\n        submitStatus.textContent = 'Saving…';\n        seasonForm.submit();\n      } catch (err) {\n        submitStatus.hidden = true;\n        submitBtn.disabled  = false;\n        alert('Image upload failed: ' + (err.message || 'Unknown error'));\n      }\n      return;\n    }\n\n    const isArtNight  = typeSelect.value === 'group_art_night';\n    const anFile      = anImageInput.files?.[0];\n\n    // If Art Night with a new file not yet uploaded, intercept and upload first\n    if (isArtNight && anFile && !anImageUrlEl.value) {\n      e.preventDefault();\n      submitBtn.disabled = true;\n      submitStatus.hidden = false;\n      submitStatus.textContent = 'Uploading image…';\n      try {\n        const year = yearInput.value || '2026';\n        const ext  = anFile.name.split('.').pop();\n        const path = `art-night-images/${year}/event-image.${ext}`;\n        const storageRef = ref(storage, path);\n        await uploadBytes(storageRef, anFile);\n        const url = await getDownloadURL(storageRef);\n        anImageUrlEl.value = url;\n        submitStatus.textContent = 'Saving…';\n        seasonForm.submit();\n      } catch (err) {\n        submitStatus.hidden = true;\n        submitBtn.disabled  = false;\n        alert('Image upload failed: ' + (err.message || 'Unknown error'));\n      }\n      return;\n    }\n\n    // If Open Mic with a new file not yet uploaded, intercept and upload first\n    if (isOpenMic && file && !omImageUrlEl.value) {\n      e.preventDefault();\n\n      submitBtn.disabled = true;\n      submitStatus.hidden = false;\n      submitStatus.textContent = 'Uploading image…';\n\n      try {\n        const year = yearInput.value || '2026';\n        const ext  = file.name.split('.').pop();\n        const path = `open-mic-images/${year}/event-image.${ext}`;\n        const storageRef = ref(storage, path);\n        await uploadBytes(storageRef, file);\n        const url = await getDownloadURL(storageRef);\n        omImageUrlEl.value = url;\n\n        submitStatus.textContent = 'Saving…';\n        seasonForm.submit();\n      } catch (err) {\n        submitStatus.hidden = true;\n        submitBtn.disabled  = false;\n        alert('Image upload failed: ' + (err.message || 'Unknown error'));\n      }\n    }\n  });\n\n  // ── Music & Movies action buttons ───────────────────────────────────────\n  document.querySelectorAll('.action-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const el       = btn;\n      const action   = el.dataset.action;\n      const shellId  = el.dataset.shellId;\n      const bandName = el.dataset.bandName || 'this band';\n\n      const msgs = {\n        confirm:  `Mark ${bandName} as confirmed for this date?`,\n        unassign: `Remove ${bandName} from this date? They will return to \"approved\" status.`,\n        publish:  'Publish this event to the public site?',\n      };\n\n      if (!confirm(msgs[action] || 'Are you sure?')) return;\n      el.disabled    = true;\n      el.textContent = 'Working…';\n\n      const res = await fetch('/api/admin/assign-band', {\n        method:  'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body:    JSON.stringify({ action, shellId }),\n      });\n\n      if (res.ok) {\n        const isRepublish = el.dataset.republish === 'true';\n        action === 'publish' && !isRepublish\n          ? (window.location.href = '/admin/events')\n          : window.location.reload();\n      } else {\n        const { error } = await res.json();\n        alert(error || 'Action failed.');\n        el.disabled    = false;\n        el.textContent = { confirm: 'Mark Confirmed', unassign: 'Unassign', publish: 'Publish ↗' }[action] ?? el.textContent;\n      }\n    });\n  });\n\n  // ── Open Mic publish buttons ────────────────────────────────────────────\n  document.querySelectorAll('.om-publish-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const el        = btn;\n      const shellId   = el.dataset.shellId;\n      const isRepublish = el.dataset.republish === 'true';\n\n      if (!confirm('Publish this Open Mic Night event to the public site?')) return;\n      el.disabled    = true;\n      el.textContent = 'Publishing…';\n\n      const res = await fetch('/api/admin/publish-open-mic', {\n        method:  'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body:    JSON.stringify({ shellId }),\n      });\n\n      if (res.ok) {\n        isRepublish ? window.location.reload() : (window.location.href = '/admin/events');\n      } else {\n        const { error } = await res.json().catch(() => ({ error: 'Unknown error' }));\n        alert(error || 'Publish failed.');\n        el.disabled    = false;\n        el.textContent = 'Publish ↗';\n      }\n    });\n  });\n\n  // ── Arts Festival publish buttons ────────────────────────────────────────\n  document.querySelectorAll('.festival-publish-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const el      = btn;\n      const shellId = el.dataset.shellId;\n\n      const isRepublish = el.dataset.republish === 'true';\n      const confirmMsg = isRepublish\n        ? 'Republish the festival page? This will update bands and info on the public site.'\n        : 'Publish this Arts Festival event to the public site?';\n      if (!confirm(confirmMsg)) return;\n      el.disabled    = true;\n      el.textContent = 'Publishing…';\n\n      try {\n        const res = await fetch('/api/admin/publish-festival', {\n          method:  'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body:    JSON.stringify({ shellId }),\n        });\n\n        if (res.ok) {\n          isRepublish ? window.location.reload() : (window.location.href = '/admin/events');\n        } else {\n          const { error } = await res.json().catch(() => ({ error: 'Unknown error' }));\n          alert(error || 'Publish failed.');\n          el.disabled    = false;\n          el.textContent = isRepublish ? 'Republish ↗' : 'Publish ↗';\n        }\n      } catch (err) {\n        alert('Publish failed: ' + (err.message || 'Unknown error'));\n        el.disabled    = false;\n        el.textContent = 'Publish ↗';\n      }\n    });\n  });\n\n  // ── Art Night publish buttons ────────────────────────────────────────────\n  document.querySelectorAll('.an-publish-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const shellId   = btn.dataset.shellId;\n      const isRepublish = btn.dataset.republish === 'true';\n      if (!confirm('Publish this Group Art Night event to the public site?')) return;\n      btn.disabled    = true;\n      btn.textContent = 'Publishing…';\n      try {\n        const res = await fetch('/api/admin/publish-art-night', {\n          method:  'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body:    JSON.stringify({ shellId }),\n        });\n        if (res.ok) {\n          isRepublish ? window.location.reload() : (window.location.href = '/admin/events');\n        } else {\n          const { error } = await res.json().catch(() => ({ error: 'Unknown error' }));\n          alert(error || 'Publish failed.');\n          btn.disabled    = false;\n          btn.textContent = 'Publish ↗';\n        }\n      } catch (err) {\n        alert('Publish failed: ' + (err.message || 'Unknown error'));\n        btn.disabled    = false;\n        btn.textContent = 'Publish ↗';\n      }\n    });\n  });\n\n  // ── Presenter assign modal ───────────────────────────────────────────────\n  const presenterModal      = document.getElementById('presenter-modal');\n  const presenterShellIdEl  = document.getElementById('presenter-modal-shell-id');\n  const presenterIdEl       = document.getElementById('presenter-modal-presenter-id');\n  const presenterNameEl     = document.getElementById('presenter-modal-presenter-name');\n  const presenterList       = document.getElementById('presenter-list');\n  const presenterSubmitBtn  = document.getElementById('presenter-modal-submit');\n  const submissions         = window.__ART_CLASS_SUBMISSIONS__ || [];\n\n  document.querySelectorAll('.btn-assign-presenter').forEach(btn => {\n    btn.addEventListener('click', () => {\n      presenterShellIdEl.value    = btn.dataset.shellId;\n      presenterIdEl.value         = '';\n      presenterNameEl.value       = '';\n      presenterSubmitBtn.disabled = true;\n\n      if (submissions.length === 0) {\n        presenterList.innerHTML = '<p class=\"dim\" style=\"font-size:13px;padding:12px\">No pending Art Class submissions found. Applicants submit via the Teach an Art Class page.</p>';\n      } else {\n        presenterList.innerHTML = submissions.map(s => `\n          <label class=\"fest-band-option\">\n            <input type=\"radio\" name=\"presenter-pick\" value=\"${s.id}\" data-name=\"${s.fullName}\" />\n            <span class=\"fest-band-option-name\">${s.fullName}</span>\n            <span class=\"fest-band-option-genre\">${s.artsArea ? s.artsArea.substring(0, 40) : ''}</span>\n          </label>\n        `).join('');\n\n        presenterList.querySelectorAll('input[type=radio]').forEach(radio => {\n          radio.addEventListener('change', () => {\n            presenterIdEl.value         = radio.value;\n            presenterNameEl.value       = radio.dataset.name;\n            presenterSubmitBtn.disabled = false;\n          });\n        });\n      }\n\n      presenterModal.hidden = false;\n    });\n  });\n\n  document.getElementById('presenter-modal-cancel')?.addEventListener('click', () => { presenterModal.hidden = true; });\n  presenterModal.addEventListener('click', e => { if (e.target === presenterModal) presenterModal.hidden = true; });\n\n  // ── Festival band assign modal ─────────────────────────────────────\n  const festModal       = document.getElementById('fest-band-modal');\n  const festSlotLabel   = document.getElementById('fest-modal-slot-label');\n  const festShellIdEl   = document.getElementById('fest-modal-shell-id');\n  const festSlotEl      = document.getElementById('fest-modal-slot');\n  const festBandIdEl    = document.getElementById('fest-modal-band-id');\n  const festBandNameEl  = document.getElementById('fest-modal-band-name');\n  const festBandList    = document.getElementById('fest-band-list');\n  const festSubmitBtn   = document.getElementById('fest-modal-submit');\n  const bands           = window.__APPROVED_BANDS__ || [];\n\n  document.querySelectorAll('.btn-assign-fest').forEach(btn => {\n    btn.addEventListener('click', () => {\n      const shellId = btn.dataset.shellId;\n      const slot    = btn.dataset.slot;\n\n      festShellIdEl.value  = shellId;\n      festSlotEl.value     = slot;\n      festSlotLabel.textContent = slot;\n      festBandIdEl.value   = '';\n      festBandNameEl.value = '';\n      festSubmitBtn.disabled = true;\n      const slotDefaults = { '1': '12:00 PM', '2': '3:00 PM', '3': '5:00 PM' };\n      document.getElementById('fest-modal-time').value = slotDefaults[slot] || '';\n\n      // Populate band list\n      if (bands.length === 0) {\n        festBandList.innerHTML = '<p class=\"dim\" style=\"font-size:13px\">No approved bands found. Approve bands on the Bands page first.</p>';\n      } else {\n        festBandList.innerHTML = bands.map(b => `\n          <label class=\"fest-band-option\">\n            <input type=\"radio\" name=\"fest-band-pick\" value=\"${b.id}\" data-name=\"${b.band_name || b.contact_name}\" />\n            <span class=\"fest-band-option-name\">${b.band_name || b.contact_name}</span>\n            ${b.genre ? `<span class=\"fest-band-option-genre\">${b.genre}</span>` : ''}\n          </label>\n        `).join('');\n\n        festBandList.querySelectorAll('input[type=radio]').forEach(radio => {\n          radio.addEventListener('change', () => {\n            festBandIdEl.value   = radio.value;\n            festBandNameEl.value = radio.dataset.name;\n            festSubmitBtn.disabled = false;\n          });\n        });\n      }\n\n      festModal.hidden = false;\n    });\n  });\n\n  document.getElementById('fest-modal-cancel')?.addEventListener('click', () => { festModal.hidden = true; });\n  festModal.addEventListener('click', e => { if (e.target === festModal) festModal.hidden = true; });\n\n  // ── Edit modal ──────────────────────────────────────────────────────────\n  const editModal = document.getElementById('edit-modal');\n\n  const IMAGE_COLLECTIONS = new Set(['open_mic_events', 'art_night_events', 'park_events']);\n\n  window.openEditModal = function(id, venue, address, startTime, collection, currentImageUrl, displayType, movieTitle, moviePreviewEmbed) {\n    document.getElementById('edit-shell-id').value   = id;\n    document.getElementById('edit-venue').value      = venue;\n    document.getElementById('edit-address').value    = address;\n    document.getElementById('edit-startTime').value  = startTime;\n    document.getElementById('edit-collection').value = collection;\n\n    // Reset image section\n    const imageSection  = document.getElementById('edit-image-section');\n    const currentImgWrap = document.getElementById('edit-current-image');\n    const currentImgEl  = document.getElementById('edit-current-img-el');\n    const editImageUrl  = document.getElementById('edit-imageUrl');\n    const editImgInput  = document.getElementById('edit-image-input');\n    const editImgPrev   = document.getElementById('edit-image-preview');\n    editImageUrl.value  = '';\n    editImgInput.value  = '';\n    editImgPrev.hidden  = true;\n\n    if (IMAGE_COLLECTIONS.has(collection)) {\n      imageSection.style.display = 'block';\n      if (currentImageUrl) {\n        currentImgEl.src          = currentImageUrl;\n        currentImgWrap.style.display = 'block';\n      } else {\n        currentImgWrap.style.display = 'none';\n      }\n    } else {\n      imageSection.style.display = 'none';\n    }\n\n    // Movie fields — only for park_events\n    const movieSection  = document.getElementById('edit-movie-section');\n    const movieFields   = document.getElementById('edit-movie-fields');\n    const displayTypeSel = document.getElementById('edit-displayType');\n    if (collection === 'park_events') {\n      movieSection.style.display = 'block';\n      displayTypeSel.value = displayType || 'music';\n      document.getElementById('edit-movieTitle').value        = movieTitle || '';\n      document.getElementById('edit-moviePreviewEmbed').value = moviePreviewEmbed || '';\n      movieFields.style.display = (displayType === 'movie' || displayType === 'both') ? 'flex' : 'none';\n    } else {\n      movieSection.style.display = 'none';\n    }\n\n    displayTypeSel.onchange = () => {\n      const v = displayTypeSel.value;\n      movieFields.style.display = (v === 'movie' || v === 'both') ? 'flex' : 'none';\n    };\n\n    editModal.hidden = false;\n  };\n\n  window.closeEditModal = function() { editModal.hidden = true; };\n\n  document.getElementById('modal-cancel-btn')?.addEventListener('click', () => { editModal.hidden = true; });\n  editModal.addEventListener('click', function(e) { if (e.target === this) editModal.hidden = true; });\n\n  // ── Edit form: upload image if new file chosen ───────────────────────────\n  const editForm      = document.getElementById('edit-form');\n  const editSaveBtn   = document.getElementById('edit-save-btn');\n  const editImgInput2 = document.getElementById('edit-image-input');\n  const editImageUrl2 = document.getElementById('edit-imageUrl');\n  const editUploadSt  = document.getElementById('edit-upload-status');\n  const editImgPrev2  = document.getElementById('edit-image-preview');\n\n  // Preview on file select\n  editImgInput2?.addEventListener('change', () => {\n    const file = editImgInput2.files?.[0];\n    if (!file) { editImgPrev2.hidden = true; return; }\n    const reader = new FileReader();\n    reader.onload = ev => {\n      editImgPrev2.hidden = false;\n      editImgPrev2.innerHTML = `<img src=\"${ev.target.result}\" alt=\"Preview\" /><span>${file.name} (${(file.size/1024/1024).toFixed(1)} MB)</span>`;\n    };\n    reader.readAsDataURL(file);\n    editImageUrl2.value = ''; // clear old URL when new file chosen\n  });\n\n  // Drag-and-drop on edit zone\n  const editZone = document.getElementById('edit-image-zone');\n  editZone?.addEventListener('dragover', e => { e.preventDefault(); editZone.classList.add('drag-over'); });\n  editZone?.addEventListener('dragleave', () => editZone.classList.remove('drag-over'));\n  editZone?.addEventListener('drop', e => {\n    e.preventDefault();\n    editZone.classList.remove('drag-over');\n    if (e.dataTransfer?.files?.length) {\n      const dt = new DataTransfer();\n      dt.items.add(e.dataTransfer.files[0]);\n      editImgInput2.files = dt.files;\n      editImgInput2.dispatchEvent(new Event('change'));\n    }\n  });\n\n  // Intercept submit: upload new image first if needed\n  editForm?.addEventListener('submit', async (e) => {\n    const file = editImgInput2?.files?.[0];\n    if (!file || editImageUrl2.value) return; // no new file, or already uploaded\n\n    e.preventDefault();\n    editSaveBtn.disabled   = true;\n    editUploadSt.hidden    = false;\n    editUploadSt.textContent = 'Uploading image…';\n\n    try {\n      const collection = document.getElementById('edit-collection').value;\n      const folder     = collection === 'open_mic_events' ? 'open-mic-images'\n                       : collection === 'park_events'      ? 'mmpark-images'\n                       : 'art-night-images';\n      const year       = new Date().getFullYear();\n      const ext        = file.name.split('.').pop();\n      const path       = `${folder}/${year}/event-image-${Date.now()}.${ext}`;\n      const storageRef = ref(storage, path);\n      await uploadBytes(storageRef, file);\n      const url = await getDownloadURL(storageRef);\n      editImageUrl2.value = url;\n      editUploadSt.textContent = 'Saving…';\n      editForm.submit();\n    } catch (err) {\n      editUploadSt.hidden    = false;\n      editUploadSt.textContent = 'Upload failed: ' + (err.message || 'Unknown error');\n      editSaveBtn.disabled   = false;\n    }\n  });\n</script> "], ["", " <!-- Inject Firebase config, approved bands, and art class submissions --> <script>(function(){", "\n  window.__FIREBASE_CONFIG__      = firebaseConfig;\n  window.__APPROVED_BANDS__       = approvedBands;\n  window.__ART_CLASS_SUBMISSIONS__ = artClassSubmissions;\n})();</script> <script type=\"module\">\n  import { initializeApp, getApps, getApp } from 'https://www.gstatic.com/firebasejs/11.6.0/firebase-app.js';\n  import { getStorage, ref, uploadBytes, getDownloadURL } from 'https://www.gstatic.com/firebasejs/11.6.0/firebase-storage.js';\n\n  const app     = getApps().length ? getApp() : initializeApp(window.__FIREBASE_CONFIG__);\n  const storage = getStorage(app);\n\n  // ── Event type dropdown ─────────────────────────────────────────────────\n  const EVENT_PRESETS = {\n    music_in_park:   { venue: 'Pleasant Green Park',        address: '3250 S 8400 W, Magna, UT 84044' },\n    open_mic:        { venue: 'Magna Library',               address: '2675 S 8950 W, Magna, UT 84044' },\n    group_art_night: { venue: 'Magna Library',               address: '2675 S 8950 W, Magna, UT 84044' },\n    annual_seminar:  { venue: 'Magna Library',               address: '2675 S 8950 W, Magna, UT 84044' },\n    arts_festival:   { venue: 'Historic Magna Main Street',  address: '8900 W to 9180 W Magna Main St, Magna, UT 84044' },\n    custom:          { venue: '',                            address: '' },\n  };\n  const PLACEHOLDER_TYPES = new Set(['annual_seminar', 'custom']);\n\n  const typeSelect    = document.getElementById('event-type-select');\n  const mainFormBody  = document.getElementById('main-form-body');\n  const placeholderEl = document.getElementById('placeholder-notice');\n  const omFields      = document.getElementById('open-mic-fields');\n  const anFields      = document.getElementById('art-night-fields');\n  const afFields      = document.getElementById('arts-festival-fields');\n  const venueInput    = document.getElementById('venue');\n  const addrInput     = document.getElementById('address');\n\n  typeSelect.addEventListener('change', () => {\n    const val = typeSelect.value;\n\n    if (!val) {\n      mainFormBody.hidden  = true;\n      placeholderEl.hidden = true;\n      return;\n    }\n    if (PLACEHOLDER_TYPES.has(val)) {\n      mainFormBody.hidden  = true;\n      placeholderEl.hidden = false;\n      return;\n    }\n\n    mainFormBody.hidden  = false;\n    placeholderEl.hidden = true;\n\n    const preset = EVENT_PRESETS[val];\n    if (preset) {\n      venueInput.value = preset.venue;\n      addrInput.value  = preset.address;\n    }\n\n    omFields.style.display = val === 'open_mic'        ? 'flex' : 'none';\n    anFields.style.display = val === 'group_art_night' ? 'flex' : 'none';\n    afFields.style.display = val === 'arts_festival'   ? 'flex' : 'none';\n  });\n\n  // ── Calendar ────────────────────────────────────────────────────────────\n  const yearInput   = document.getElementById('year');\n  const calGrid     = document.getElementById('cal-grid');\n  const datesHidden = document.getElementById('dates-hidden');\n  const chipsWrap   = document.getElementById('selected-chips');\n  const chipsList   = document.getElementById('chips-list');\n  const clearBtn    = document.getElementById('clear-dates-btn');\n\n  const MONTH_NAMES = ['January','February','March','April','May','June',\n                       'July','August','September','October','November','December'];\n  const DAY_ABBRS   = ['Su','Mo','Tu','We','Th','Fr','Sa'];\n\n  let selectedDates = new Set();\n\n  function pad(n) { return String(n).padStart(2, '0'); }\n  function dateKey(y, m, d) { return \\`\\${y}-\\${pad(m+1)}-\\${pad(d)}\\`; }\n\n  function buildCalendar(year) {\n    calGrid.innerHTML = '';\n    for (let m = 0; m < 12; m++) {\n      const monthEl = document.createElement('div');\n      monthEl.className = 'cal-month';\n\n      const heading = document.createElement('div');\n      heading.className = 'cal-month-heading';\n      heading.textContent = MONTH_NAMES[m];\n      monthEl.appendChild(heading);\n\n      const dowRow = document.createElement('div');\n      dowRow.className = 'cal-dow-row';\n      dowRow.style.cssText = 'display:grid;grid-template-columns:repeat(7,1fr);gap:2px;margin-bottom:2px;';\n      DAY_ABBRS.forEach(ab => {\n        const c = document.createElement('div');\n        c.className = 'cal-dow';\n        c.textContent = ab;\n        dowRow.appendChild(c);\n      });\n      monthEl.appendChild(dowRow);\n\n      const daysEl = document.createElement('div');\n      daysEl.className = 'cal-days';\n      daysEl.style.cssText = 'display:grid;grid-template-columns:repeat(7,1fr);gap:2px;';\n\n      const firstDow   = new Date(year, m, 1).getDay();\n      const daysInMonth = new Date(year, m+1, 0).getDate();\n\n      for (let i = 0; i < firstDow; i++) {\n        const blank = document.createElement('div');\n        blank.className = 'cal-day cal-blank';\n        daysEl.appendChild(blank);\n      }\n\n      for (let d = 1; d <= daysInMonth; d++) {\n        const key   = dateKey(year, m, d);\n        const dayEl = document.createElement('div');\n        dayEl.className = 'cal-day';\n        dayEl.textContent = d;\n        dayEl.dataset.date = key;\n        if (selectedDates.has(key)) dayEl.classList.add('cal-selected');\n\n        dayEl.addEventListener('click', () => {\n          if (selectedDates.has(key)) {\n            selectedDates.delete(key);\n            dayEl.classList.remove('cal-selected');\n          } else {\n            selectedDates.add(key);\n            dayEl.classList.add('cal-selected');\n          }\n          syncDates();\n        });\n\n        daysEl.appendChild(dayEl);\n      }\n\n      monthEl.appendChild(daysEl);\n      calGrid.appendChild(monthEl);\n    }\n  }\n\n  function syncDates() {\n    const sorted = Array.from(selectedDates).sort();\n    datesHidden.value = sorted.join('\\\\n');\n\n    if (sorted.length === 0) {\n      chipsWrap.hidden = true;\n      chipsList.innerHTML = '';\n      return;\n    }\n\n    chipsWrap.hidden = false;\n    chipsList.innerHTML = '';\n    sorted.forEach(key => {\n      const [y, mo, d] = key.split('-').map(Number);\n      const label = new Date(y, mo-1, d).toLocaleDateString('en-US', { month: 'short', day: 'numeric' });\n      const chip = document.createElement('span');\n      chip.className = 'date-chip';\n      chip.innerHTML = \\`\\${label} <button type=\"button\" class=\"chip-remove\" data-date=\"\\${key}\" aria-label=\"Remove \\${label}\">×</button>\\`;\n      chipsList.appendChild(chip);\n    });\n\n    chipsList.querySelectorAll('.chip-remove').forEach(btn => {\n      btn.addEventListener('click', () => {\n        const key = btn.dataset.date;\n        selectedDates.delete(key);\n        const dayEl = calGrid.querySelector(\\`[data-date=\"\\${key}\"]\\`);\n        if (dayEl) dayEl.classList.remove('cal-selected');\n        syncDates();\n      });\n    });\n  }\n\n  clearBtn.addEventListener('click', () => {\n    selectedDates.clear();\n    calGrid.querySelectorAll('.cal-selected').forEach(el => el.classList.remove('cal-selected'));\n    syncDates();\n  });\n\n  buildCalendar(parseInt(yearInput.value) || new Date().getFullYear());\n  yearInput.addEventListener('change', () => {\n    selectedDates.clear();\n    syncDates();\n    buildCalendar(parseInt(yearInput.value) || new Date().getFullYear());\n  });\n\n  // ── Art Night image upload ─────────────────────────────────────────────\n  const anImageZone    = document.getElementById('an-image-zone');\n  const anImageInput   = document.getElementById('an-image-input');\n  const anImagePreview = document.getElementById('an-image-preview');\n  const anImageUrlEl   = document.getElementById('anImageUrl');\n  const anUploadStatus = document.getElementById('an-upload-status');\n\n  anImageZone.addEventListener('dragover', e => { e.preventDefault(); anImageZone.classList.add('drag-over'); });\n  anImageZone.addEventListener('dragleave', () => anImageZone.classList.remove('drag-over'));\n  anImageZone.addEventListener('drop', e => {\n    e.preventDefault();\n    anImageZone.classList.remove('drag-over');\n    if (e.dataTransfer?.files?.length) {\n      const dt = new DataTransfer();\n      dt.items.add(e.dataTransfer.files[0]);\n      anImageInput.files = dt.files;\n      anImageInput.dispatchEvent(new Event('change'));\n    }\n  });\n\n  anImageInput.addEventListener('change', () => {\n    const file = anImageInput.files?.[0];\n    if (!file) { anImagePreview.hidden = true; return; }\n    const reader = new FileReader();\n    reader.onload = ev => {\n      anImagePreview.hidden = false;\n      anImagePreview.innerHTML = \\`<img src=\"\\${ev.target.result}\" alt=\"Preview\" /><span>\\${file.name} (\\${(file.size/1024/1024).toFixed(1)} MB)</span>\\`;\n    };\n    reader.readAsDataURL(file);\n    anImageUrlEl.value = '';\n  });\n\n  // ── Open Mic image upload ───────────────────────────────────────────────\n  const omImageZone    = document.getElementById('om-image-zone');\n  const omImageInput   = document.getElementById('om-image-input');\n  const omImagePreview = document.getElementById('om-image-preview');\n  const omImageUrlEl   = document.getElementById('omImageUrl');\n  const omUploadStatus = document.getElementById('om-upload-status');\n\n  omImageZone.addEventListener('dragover', e => { e.preventDefault(); omImageZone.classList.add('drag-over'); });\n  omImageZone.addEventListener('dragleave', () => omImageZone.classList.remove('drag-over'));\n  omImageZone.addEventListener('drop', e => {\n    e.preventDefault();\n    omImageZone.classList.remove('drag-over');\n    if (e.dataTransfer?.files?.length) {\n      const dt = new DataTransfer();\n      dt.items.add(e.dataTransfer.files[0]);\n      omImageInput.files = dt.files;\n      omImageInput.dispatchEvent(new Event('change'));\n    }\n  });\n\n  omImageInput.addEventListener('change', () => {\n    const file = omImageInput.files?.[0];\n    if (!file) { omImagePreview.hidden = true; return; }\n    const reader = new FileReader();\n    reader.onload = ev => {\n      omImagePreview.hidden = false;\n      omImagePreview.innerHTML = \\`<img src=\"\\${ev.target.result}\" alt=\"Preview\" /><span>\\${file.name} (\\${(file.size/1024/1024).toFixed(1)} MB)</span>\\`;\n    };\n    reader.readAsDataURL(file);\n    omImageUrlEl.value = ''; // clear previously uploaded URL when new file chosen\n  });\n\n  // ── Form submit — upload image first if needed, then POST ───────────────\n  const seasonForm   = document.getElementById('season-form');\n  const submitBtn    = document.getElementById('submit-btn');\n  const submitStatus = document.getElementById('submit-status');\n\n  seasonForm.addEventListener('submit', async (e) => {\n    const isOpenMic = typeSelect.value === 'open_mic';\n    const file      = omImageInput.files?.[0];\n\n    // Validate dates for all active event types\n    if (mainFormBody && !mainFormBody.hidden && !datesHidden.value.trim()) {\n      e.preventDefault();\n      alert('Please select at least one date from the calendar.');\n      return;\n    }\n\n    const isMMP      = typeSelect.value === 'music_in_park';\n    const mmpFile    = document.getElementById('mmp-image-input')?.files?.[0];\n    const mmpUrlEl   = document.getElementById('mmpImageUrl');\n\n    if (isMMP && mmpFile && !mmpUrlEl?.value) {\n      e.preventDefault();\n      submitBtn.disabled = true;\n      submitStatus.hidden = false;\n      submitStatus.textContent = 'Uploading image…';\n      try {\n        const year = yearInput.value || '2026';\n        const ext  = mmpFile.name.split('.').pop();\n        const path = \\`mmpark-images/\\${year}/event-image.\\${ext}\\`;\n        const storageRef = ref(storage, path);\n        await uploadBytes(storageRef, mmpFile);\n        const url = await getDownloadURL(storageRef);\n        mmpUrlEl.value = url;\n        submitStatus.textContent = 'Saving…';\n        seasonForm.submit();\n      } catch (err) {\n        submitStatus.hidden = true;\n        submitBtn.disabled  = false;\n        alert('Image upload failed: ' + (err.message || 'Unknown error'));\n      }\n      return;\n    }\n\n    const isArtNight  = typeSelect.value === 'group_art_night';\n    const anFile      = anImageInput.files?.[0];\n\n    // If Art Night with a new file not yet uploaded, intercept and upload first\n    if (isArtNight && anFile && !anImageUrlEl.value) {\n      e.preventDefault();\n      submitBtn.disabled = true;\n      submitStatus.hidden = false;\n      submitStatus.textContent = 'Uploading image…';\n      try {\n        const year = yearInput.value || '2026';\n        const ext  = anFile.name.split('.').pop();\n        const path = \\`art-night-images/\\${year}/event-image.\\${ext}\\`;\n        const storageRef = ref(storage, path);\n        await uploadBytes(storageRef, anFile);\n        const url = await getDownloadURL(storageRef);\n        anImageUrlEl.value = url;\n        submitStatus.textContent = 'Saving…';\n        seasonForm.submit();\n      } catch (err) {\n        submitStatus.hidden = true;\n        submitBtn.disabled  = false;\n        alert('Image upload failed: ' + (err.message || 'Unknown error'));\n      }\n      return;\n    }\n\n    // If Open Mic with a new file not yet uploaded, intercept and upload first\n    if (isOpenMic && file && !omImageUrlEl.value) {\n      e.preventDefault();\n\n      submitBtn.disabled = true;\n      submitStatus.hidden = false;\n      submitStatus.textContent = 'Uploading image…';\n\n      try {\n        const year = yearInput.value || '2026';\n        const ext  = file.name.split('.').pop();\n        const path = \\`open-mic-images/\\${year}/event-image.\\${ext}\\`;\n        const storageRef = ref(storage, path);\n        await uploadBytes(storageRef, file);\n        const url = await getDownloadURL(storageRef);\n        omImageUrlEl.value = url;\n\n        submitStatus.textContent = 'Saving…';\n        seasonForm.submit();\n      } catch (err) {\n        submitStatus.hidden = true;\n        submitBtn.disabled  = false;\n        alert('Image upload failed: ' + (err.message || 'Unknown error'));\n      }\n    }\n  });\n\n  // ── Music & Movies action buttons ───────────────────────────────────────\n  document.querySelectorAll('.action-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const el       = btn;\n      const action   = el.dataset.action;\n      const shellId  = el.dataset.shellId;\n      const bandName = el.dataset.bandName || 'this band';\n\n      const msgs = {\n        confirm:  \\`Mark \\${bandName} as confirmed for this date?\\`,\n        unassign: \\`Remove \\${bandName} from this date? They will return to \"approved\" status.\\`,\n        publish:  'Publish this event to the public site?',\n      };\n\n      if (!confirm(msgs[action] || 'Are you sure?')) return;\n      el.disabled    = true;\n      el.textContent = 'Working…';\n\n      const res = await fetch('/api/admin/assign-band', {\n        method:  'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body:    JSON.stringify({ action, shellId }),\n      });\n\n      if (res.ok) {\n        const isRepublish = el.dataset.republish === 'true';\n        action === 'publish' && !isRepublish\n          ? (window.location.href = '/admin/events')\n          : window.location.reload();\n      } else {\n        const { error } = await res.json();\n        alert(error || 'Action failed.');\n        el.disabled    = false;\n        el.textContent = { confirm: 'Mark Confirmed', unassign: 'Unassign', publish: 'Publish ↗' }[action] ?? el.textContent;\n      }\n    });\n  });\n\n  // ── Open Mic publish buttons ────────────────────────────────────────────\n  document.querySelectorAll('.om-publish-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const el        = btn;\n      const shellId   = el.dataset.shellId;\n      const isRepublish = el.dataset.republish === 'true';\n\n      if (!confirm('Publish this Open Mic Night event to the public site?')) return;\n      el.disabled    = true;\n      el.textContent = 'Publishing…';\n\n      const res = await fetch('/api/admin/publish-open-mic', {\n        method:  'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body:    JSON.stringify({ shellId }),\n      });\n\n      if (res.ok) {\n        isRepublish ? window.location.reload() : (window.location.href = '/admin/events');\n      } else {\n        const { error } = await res.json().catch(() => ({ error: 'Unknown error' }));\n        alert(error || 'Publish failed.');\n        el.disabled    = false;\n        el.textContent = 'Publish ↗';\n      }\n    });\n  });\n\n  // ── Arts Festival publish buttons ────────────────────────────────────────\n  document.querySelectorAll('.festival-publish-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const el      = btn;\n      const shellId = el.dataset.shellId;\n\n      const isRepublish = el.dataset.republish === 'true';\n      const confirmMsg = isRepublish\n        ? 'Republish the festival page? This will update bands and info on the public site.'\n        : 'Publish this Arts Festival event to the public site?';\n      if (!confirm(confirmMsg)) return;\n      el.disabled    = true;\n      el.textContent = 'Publishing…';\n\n      try {\n        const res = await fetch('/api/admin/publish-festival', {\n          method:  'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body:    JSON.stringify({ shellId }),\n        });\n\n        if (res.ok) {\n          isRepublish ? window.location.reload() : (window.location.href = '/admin/events');\n        } else {\n          const { error } = await res.json().catch(() => ({ error: 'Unknown error' }));\n          alert(error || 'Publish failed.');\n          el.disabled    = false;\n          el.textContent = isRepublish ? 'Republish ↗' : 'Publish ↗';\n        }\n      } catch (err) {\n        alert('Publish failed: ' + (err.message || 'Unknown error'));\n        el.disabled    = false;\n        el.textContent = 'Publish ↗';\n      }\n    });\n  });\n\n  // ── Art Night publish buttons ────────────────────────────────────────────\n  document.querySelectorAll('.an-publish-btn').forEach(btn => {\n    btn.addEventListener('click', async () => {\n      const shellId   = btn.dataset.shellId;\n      const isRepublish = btn.dataset.republish === 'true';\n      if (!confirm('Publish this Group Art Night event to the public site?')) return;\n      btn.disabled    = true;\n      btn.textContent = 'Publishing…';\n      try {\n        const res = await fetch('/api/admin/publish-art-night', {\n          method:  'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body:    JSON.stringify({ shellId }),\n        });\n        if (res.ok) {\n          isRepublish ? window.location.reload() : (window.location.href = '/admin/events');\n        } else {\n          const { error } = await res.json().catch(() => ({ error: 'Unknown error' }));\n          alert(error || 'Publish failed.');\n          btn.disabled    = false;\n          btn.textContent = 'Publish ↗';\n        }\n      } catch (err) {\n        alert('Publish failed: ' + (err.message || 'Unknown error'));\n        btn.disabled    = false;\n        btn.textContent = 'Publish ↗';\n      }\n    });\n  });\n\n  // ── Presenter assign modal ───────────────────────────────────────────────\n  const presenterModal      = document.getElementById('presenter-modal');\n  const presenterShellIdEl  = document.getElementById('presenter-modal-shell-id');\n  const presenterIdEl       = document.getElementById('presenter-modal-presenter-id');\n  const presenterNameEl     = document.getElementById('presenter-modal-presenter-name');\n  const presenterList       = document.getElementById('presenter-list');\n  const presenterSubmitBtn  = document.getElementById('presenter-modal-submit');\n  const submissions         = window.__ART_CLASS_SUBMISSIONS__ || [];\n\n  document.querySelectorAll('.btn-assign-presenter').forEach(btn => {\n    btn.addEventListener('click', () => {\n      presenterShellIdEl.value    = btn.dataset.shellId;\n      presenterIdEl.value         = '';\n      presenterNameEl.value       = '';\n      presenterSubmitBtn.disabled = true;\n\n      if (submissions.length === 0) {\n        presenterList.innerHTML = '<p class=\"dim\" style=\"font-size:13px;padding:12px\">No pending Art Class submissions found. Applicants submit via the Teach an Art Class page.</p>';\n      } else {\n        presenterList.innerHTML = submissions.map(s => \\`\n          <label class=\"fest-band-option\">\n            <input type=\"radio\" name=\"presenter-pick\" value=\"\\${s.id}\" data-name=\"\\${s.fullName}\" />\n            <span class=\"fest-band-option-name\">\\${s.fullName}</span>\n            <span class=\"fest-band-option-genre\">\\${s.artsArea ? s.artsArea.substring(0, 40) : ''}</span>\n          </label>\n        \\`).join('');\n\n        presenterList.querySelectorAll('input[type=radio]').forEach(radio => {\n          radio.addEventListener('change', () => {\n            presenterIdEl.value         = radio.value;\n            presenterNameEl.value       = radio.dataset.name;\n            presenterSubmitBtn.disabled = false;\n          });\n        });\n      }\n\n      presenterModal.hidden = false;\n    });\n  });\n\n  document.getElementById('presenter-modal-cancel')?.addEventListener('click', () => { presenterModal.hidden = true; });\n  presenterModal.addEventListener('click', e => { if (e.target === presenterModal) presenterModal.hidden = true; });\n\n  // ── Festival band assign modal ─────────────────────────────────────\n  const festModal       = document.getElementById('fest-band-modal');\n  const festSlotLabel   = document.getElementById('fest-modal-slot-label');\n  const festShellIdEl   = document.getElementById('fest-modal-shell-id');\n  const festSlotEl      = document.getElementById('fest-modal-slot');\n  const festBandIdEl    = document.getElementById('fest-modal-band-id');\n  const festBandNameEl  = document.getElementById('fest-modal-band-name');\n  const festBandList    = document.getElementById('fest-band-list');\n  const festSubmitBtn   = document.getElementById('fest-modal-submit');\n  const bands           = window.__APPROVED_BANDS__ || [];\n\n  document.querySelectorAll('.btn-assign-fest').forEach(btn => {\n    btn.addEventListener('click', () => {\n      const shellId = btn.dataset.shellId;\n      const slot    = btn.dataset.slot;\n\n      festShellIdEl.value  = shellId;\n      festSlotEl.value     = slot;\n      festSlotLabel.textContent = slot;\n      festBandIdEl.value   = '';\n      festBandNameEl.value = '';\n      festSubmitBtn.disabled = true;\n      const slotDefaults = { '1': '12:00 PM', '2': '3:00 PM', '3': '5:00 PM' };\n      document.getElementById('fest-modal-time').value = slotDefaults[slot] || '';\n\n      // Populate band list\n      if (bands.length === 0) {\n        festBandList.innerHTML = '<p class=\"dim\" style=\"font-size:13px\">No approved bands found. Approve bands on the Bands page first.</p>';\n      } else {\n        festBandList.innerHTML = bands.map(b => \\`\n          <label class=\"fest-band-option\">\n            <input type=\"radio\" name=\"fest-band-pick\" value=\"\\${b.id}\" data-name=\"\\${b.band_name || b.contact_name}\" />\n            <span class=\"fest-band-option-name\">\\${b.band_name || b.contact_name}</span>\n            \\${b.genre ? \\`<span class=\"fest-band-option-genre\">\\${b.genre}</span>\\` : ''}\n          </label>\n        \\`).join('');\n\n        festBandList.querySelectorAll('input[type=radio]').forEach(radio => {\n          radio.addEventListener('change', () => {\n            festBandIdEl.value   = radio.value;\n            festBandNameEl.value = radio.dataset.name;\n            festSubmitBtn.disabled = false;\n          });\n        });\n      }\n\n      festModal.hidden = false;\n    });\n  });\n\n  document.getElementById('fest-modal-cancel')?.addEventListener('click', () => { festModal.hidden = true; });\n  festModal.addEventListener('click', e => { if (e.target === festModal) festModal.hidden = true; });\n\n  // ── Edit modal ──────────────────────────────────────────────────────────\n  const editModal = document.getElementById('edit-modal');\n\n  const IMAGE_COLLECTIONS = new Set(['open_mic_events', 'art_night_events', 'park_events']);\n\n  window.openEditModal = function(id, venue, address, startTime, collection, currentImageUrl, displayType, movieTitle, moviePreviewEmbed) {\n    document.getElementById('edit-shell-id').value   = id;\n    document.getElementById('edit-venue').value      = venue;\n    document.getElementById('edit-address').value    = address;\n    document.getElementById('edit-startTime').value  = startTime;\n    document.getElementById('edit-collection').value = collection;\n\n    // Reset image section\n    const imageSection  = document.getElementById('edit-image-section');\n    const currentImgWrap = document.getElementById('edit-current-image');\n    const currentImgEl  = document.getElementById('edit-current-img-el');\n    const editImageUrl  = document.getElementById('edit-imageUrl');\n    const editImgInput  = document.getElementById('edit-image-input');\n    const editImgPrev   = document.getElementById('edit-image-preview');\n    editImageUrl.value  = '';\n    editImgInput.value  = '';\n    editImgPrev.hidden  = true;\n\n    if (IMAGE_COLLECTIONS.has(collection)) {\n      imageSection.style.display = 'block';\n      if (currentImageUrl) {\n        currentImgEl.src          = currentImageUrl;\n        currentImgWrap.style.display = 'block';\n      } else {\n        currentImgWrap.style.display = 'none';\n      }\n    } else {\n      imageSection.style.display = 'none';\n    }\n\n    // Movie fields — only for park_events\n    const movieSection  = document.getElementById('edit-movie-section');\n    const movieFields   = document.getElementById('edit-movie-fields');\n    const displayTypeSel = document.getElementById('edit-displayType');\n    if (collection === 'park_events') {\n      movieSection.style.display = 'block';\n      displayTypeSel.value = displayType || 'music';\n      document.getElementById('edit-movieTitle').value        = movieTitle || '';\n      document.getElementById('edit-moviePreviewEmbed').value = moviePreviewEmbed || '';\n      movieFields.style.display = (displayType === 'movie' || displayType === 'both') ? 'flex' : 'none';\n    } else {\n      movieSection.style.display = 'none';\n    }\n\n    displayTypeSel.onchange = () => {\n      const v = displayTypeSel.value;\n      movieFields.style.display = (v === 'movie' || v === 'both') ? 'flex' : 'none';\n    };\n\n    editModal.hidden = false;\n  };\n\n  window.closeEditModal = function() { editModal.hidden = true; };\n\n  document.getElementById('modal-cancel-btn')?.addEventListener('click', () => { editModal.hidden = true; });\n  editModal.addEventListener('click', function(e) { if (e.target === this) editModal.hidden = true; });\n\n  // ── Edit form: upload image if new file chosen ───────────────────────────\n  const editForm      = document.getElementById('edit-form');\n  const editSaveBtn   = document.getElementById('edit-save-btn');\n  const editImgInput2 = document.getElementById('edit-image-input');\n  const editImageUrl2 = document.getElementById('edit-imageUrl');\n  const editUploadSt  = document.getElementById('edit-upload-status');\n  const editImgPrev2  = document.getElementById('edit-image-preview');\n\n  // Preview on file select\n  editImgInput2?.addEventListener('change', () => {\n    const file = editImgInput2.files?.[0];\n    if (!file) { editImgPrev2.hidden = true; return; }\n    const reader = new FileReader();\n    reader.onload = ev => {\n      editImgPrev2.hidden = false;\n      editImgPrev2.innerHTML = \\`<img src=\"\\${ev.target.result}\" alt=\"Preview\" /><span>\\${file.name} (\\${(file.size/1024/1024).toFixed(1)} MB)</span>\\`;\n    };\n    reader.readAsDataURL(file);\n    editImageUrl2.value = ''; // clear old URL when new file chosen\n  });\n\n  // Drag-and-drop on edit zone\n  const editZone = document.getElementById('edit-image-zone');\n  editZone?.addEventListener('dragover', e => { e.preventDefault(); editZone.classList.add('drag-over'); });\n  editZone?.addEventListener('dragleave', () => editZone.classList.remove('drag-over'));\n  editZone?.addEventListener('drop', e => {\n    e.preventDefault();\n    editZone.classList.remove('drag-over');\n    if (e.dataTransfer?.files?.length) {\n      const dt = new DataTransfer();\n      dt.items.add(e.dataTransfer.files[0]);\n      editImgInput2.files = dt.files;\n      editImgInput2.dispatchEvent(new Event('change'));\n    }\n  });\n\n  // Intercept submit: upload new image first if needed\n  editForm?.addEventListener('submit', async (e) => {\n    const file = editImgInput2?.files?.[0];\n    if (!file || editImageUrl2.value) return; // no new file, or already uploaded\n\n    e.preventDefault();\n    editSaveBtn.disabled   = true;\n    editUploadSt.hidden    = false;\n    editUploadSt.textContent = 'Uploading image…';\n\n    try {\n      const collection = document.getElementById('edit-collection').value;\n      const folder     = collection === 'open_mic_events' ? 'open-mic-images'\n                       : collection === 'park_events'      ? 'mmpark-images'\n                       : 'art-night-images';\n      const year       = new Date().getFullYear();\n      const ext        = file.name.split('.').pop();\n      const path       = \\`\\${folder}/\\${year}/event-image-\\${Date.now()}.\\${ext}\\`;\n      const storageRef = ref(storage, path);\n      await uploadBytes(storageRef, file);\n      const url = await getDownloadURL(storageRef);\n      editImageUrl2.value = url;\n      editUploadSt.textContent = 'Saving…';\n      editForm.submit();\n    } catch (err) {\n      editUploadSt.hidden    = false;\n      editUploadSt.textContent = 'Upload failed: ' + (err.message || 'Unknown error');\n      editSaveBtn.disabled   = false;\n    }\n  });\n</script> "])), renderComponent($$result, "AdminLayout", $$AdminLayout, { "title": "Plan Events", "user": user, "data-astro-cid-szbgt4gh": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<div class="page-header" data-astro-cid-szbgt4gh> <h1 data-astro-cid-szbgt4gh>Plan Events</h1> <p class="page-sub" data-astro-cid-szbgt4gh>Select an event to set up a new series and generate event shells.</p> </div> ${flashMsg && renderTemplate`<div${addAttribute(`flash flash-${flashType}`, "class")} data-astro-cid-szbgt4gh>${flashMsg}</div>`} <section class="card" data-astro-cid-szbgt4gh> <h2 class="card-title" data-astro-cid-szbgt4gh>Set Up a New Event Series</h2> <p class="card-sub" data-astro-cid-szbgt4gh>Choose an event, fill in the details, and select dates from the calendar. A shell will be created for each selected date.</p> <form method="POST" class="season-form" id="season-form" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="create_season" data-astro-cid-szbgt4gh> <!-- Hidden dates field — written by the calendar JS --> <textarea name="dates" id="dates-hidden" style="display:none" readonly data-astro-cid-szbgt4gh></textarea> <!-- Event title dropdown --> <div class="form-field" data-astro-cid-szbgt4gh> <label for="event-type-select" data-astro-cid-szbgt4gh>Event Title <span class="req" data-astro-cid-szbgt4gh>*</span></label> <select id="event-type-select" name="seriesType" data-astro-cid-szbgt4gh> <option value="" data-astro-cid-szbgt4gh>— Select an event —</option> <option value="music_in_park" data-astro-cid-szbgt4gh>Music &amp; Movies in the Park</option> <option value="open_mic" data-astro-cid-szbgt4gh>Open Mic Night</option> <option value="group_art_night" data-astro-cid-szbgt4gh>Group Art Night</option> <option value="annual_seminar" data-astro-cid-szbgt4gh>Annual Arts Seminar</option> <option value="arts_festival" data-astro-cid-szbgt4gh>Magna Main Street Arts Festival</option> <option value="custom" data-astro-cid-szbgt4gh>— or something else —</option> </select> </div> <!-- Main form body (music, open mic, art night) --> <div id="main-form-body" hidden data-astro-cid-szbgt4gh> <div class="form-row" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="year" data-astro-cid-szbgt4gh>Season Year <span class="req" data-astro-cid-szbgt4gh>*</span></label> <input type="number" id="year" name="year" value="2026" min="2024" max="2040" data-astro-cid-szbgt4gh> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="startTime" data-astro-cid-szbgt4gh>Default Start Time <span class="req" data-astro-cid-szbgt4gh>*</span></label> <input type="text" id="startTime" name="startTime" placeholder="7:00 PM" data-astro-cid-szbgt4gh> </div> </div> <div class="form-row" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="venue" data-astro-cid-szbgt4gh>Venue Name <span class="req" data-astro-cid-szbgt4gh>*</span></label> <input type="text" id="venue" name="venue" placeholder="Venue" data-astro-cid-szbgt4gh> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="address" data-astro-cid-szbgt4gh>Address <span class="req" data-astro-cid-szbgt4gh>*</span></label> <input type="text" id="address" name="address" placeholder="Address" data-astro-cid-szbgt4gh> </div> </div> <!-- Open Mic–specific fields --> <div id="open-mic-fields" class="event-extra-fields" style="display:none;" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="omTitle" data-astro-cid-szbgt4gh>Event Title</label> <input type="text" id="omTitle" name="omTitle" value="Open Mic Night" placeholder="Open Mic Night" data-astro-cid-szbgt4gh> </div> <!-- Image upload --> <div class="form-field" data-astro-cid-szbgt4gh> <label data-astro-cid-szbgt4gh>
Event Image
<span class="field-hint" data-astro-cid-szbgt4gh>One image used for all dates in this series</span> </label> <!-- Populated by JS after upload to Firebase Storage --> <input type="hidden" id="omImageUrl" name="omImageUrl" data-astro-cid-szbgt4gh> <div class="upload-zone" id="om-image-zone" data-astro-cid-szbgt4gh> <input type="file" id="om-image-input" accept="image/jpeg,image/png,image/webp" class="upload-input" data-astro-cid-szbgt4gh> <label for="om-image-input" class="upload-label" data-astro-cid-szbgt4gh> <span class="upload-icon" data-astro-cid-szbgt4gh>🖼️</span> <span class="upload-text" data-astro-cid-szbgt4gh><strong data-astro-cid-szbgt4gh>Choose an image</strong> or drag and drop</span> <span class="upload-hint" data-astro-cid-szbgt4gh>JPG, PNG or WebP · max 10 MB</span> </label> <div class="upload-preview" id="om-image-preview" hidden data-astro-cid-szbgt4gh></div> </div> <div id="om-upload-status" class="upload-status" hidden data-astro-cid-szbgt4gh></div> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="omDescription" data-astro-cid-szbgt4gh>Description</label> <textarea id="omDescription" name="omDescription" rows="3" placeholder="Join us for an evening of music, poetry, and performance. All are welcome to perform or just enjoy the show." data-astro-cid-szbgt4gh></textarea> </div> </div> <!-- Arts Festival–specific fields --> <div id="arts-festival-fields" class="event-extra-fields" style="display:none;" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="afTitle" data-astro-cid-szbgt4gh>Event Title</label> <input type="text" id="afTitle" name="afTitle" value="Magna Main Street Arts Festival" data-astro-cid-szbgt4gh> </div> <div class="form-row" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="afStartTime" data-astro-cid-szbgt4gh>Start Time <span class="req" data-astro-cid-szbgt4gh>*</span></label> <input type="text" id="afStartTime" name="afStartTime" placeholder="10:00 AM" data-astro-cid-szbgt4gh> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="afEndTime" data-astro-cid-szbgt4gh>End Time <span class="req" data-astro-cid-szbgt4gh>*</span></label> <input type="text" id="afEndTime" name="afEndTime" placeholder="6:00 PM" data-astro-cid-szbgt4gh> </div> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="afDescription" data-astro-cid-szbgt4gh>Description <span class="field-hint" data-astro-cid-szbgt4gh>(shown on the public event page)</span></label> <textarea id="afDescription" name="afDescription" rows="4" placeholder="Our annual flagship event draws 5,000–7,000 people to Historic Magna Main Street for a full day of live music, a fine arts contest, street performers, and arts and food vendors. Free and open to all." data-astro-cid-szbgt4gh></textarea> </div> <div class="info-callout" data-astro-cid-szbgt4gh> <span data-astro-cid-szbgt4gh>ℹ️</span> <span data-astro-cid-szbgt4gh>A single event shell will be created. After publishing, approved vendor applications will appear on the public festival page automatically.</span> </div> </div> <!-- Group Art Night–specific fields --> <div id="art-night-fields" class="event-extra-fields" style="display:none;" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="anTitle" data-astro-cid-szbgt4gh>Event Title</label> <input type="text" id="anTitle" name="anTitle" value="Group Art Night" placeholder="Group Art Night" data-astro-cid-szbgt4gh> </div> <!-- Image upload --> <div class="form-field" data-astro-cid-szbgt4gh> <label data-astro-cid-szbgt4gh>
Event Image
<span class="field-hint" data-astro-cid-szbgt4gh>One image used for all dates in this series</span> </label> <input type="hidden" id="anImageUrl" name="anImageUrl" data-astro-cid-szbgt4gh> <div class="upload-zone" id="an-image-zone" data-astro-cid-szbgt4gh> <input type="file" id="an-image-input" accept="image/jpeg,image/png,image/webp" class="upload-input" data-astro-cid-szbgt4gh> <label for="an-image-input" class="upload-label" data-astro-cid-szbgt4gh> <span class="upload-icon" data-astro-cid-szbgt4gh>🖼️</span> <span class="upload-text" data-astro-cid-szbgt4gh><strong data-astro-cid-szbgt4gh>Choose an image</strong> or drag and drop</span> <span class="upload-hint" data-astro-cid-szbgt4gh>JPG, PNG or WebP · max 10 MB</span> </label> <div class="upload-preview" id="an-image-preview" hidden data-astro-cid-szbgt4gh></div> </div> <div id="an-upload-status" class="upload-status" hidden data-astro-cid-szbgt4gh></div> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="anDescription" data-astro-cid-szbgt4gh>Description</label> <textarea id="anDescription" name="anDescription" rows="3" placeholder="A community art session guided by a local artist. All skill levels welcome." data-astro-cid-szbgt4gh></textarea> </div> <div class="info-callout" data-astro-cid-szbgt4gh> <span data-astro-cid-szbgt4gh>ℹ️</span> <span data-astro-cid-szbgt4gh>After creating shells, use the table below to assign a presenter. Click <strong data-astro-cid-szbgt4gh>Assign Presenter</strong> on any shell to pick from pending Art Class submissions. Once assigned, use <strong data-astro-cid-szbgt4gh>Mark Confirmed</strong> when you've spoken with the presenter, then <strong data-astro-cid-szbgt4gh>Publish ↗</strong> to push the event live.</span> </div> </div> <!-- ── DATE CALENDAR ─────────────────────────────────────────── --> <div class="form-field" data-astro-cid-szbgt4gh> <label data-astro-cid-szbgt4gh>
Event Dates <span class="req" data-astro-cid-szbgt4gh>*</span> <span class="field-hint" data-astro-cid-szbgt4gh>Click dates to toggle them on/off</span> </label> <!-- Selected date chips summary --> <div id="selected-chips" class="selected-chips" hidden data-astro-cid-szbgt4gh> <span class="chips-label" data-astro-cid-szbgt4gh>Selected:</span> <div id="chips-list" class="chips-list" data-astro-cid-szbgt4gh></div> <button type="button" id="clear-dates-btn" class="clear-dates-btn" data-astro-cid-szbgt4gh>Clear all</button> </div> <!-- 12-month scrollable calendar --> <div id="cal-grid" class="cal-grid" data-astro-cid-szbgt4gh></div> </div> <div class="form-actions" data-astro-cid-szbgt4gh> <button type="submit" id="submit-btn" class="btn btn-primary" data-astro-cid-szbgt4gh>Generate Event Shells →</button> <span id="submit-status" class="submit-status" hidden data-astro-cid-szbgt4gh></span> </div> </div><!-- /main-form-body --> </form> </section>  ${Object.entries(shellsBySeason).sort(([a], [b]) => Number(b) - Number(a)).map(([yr, shells]) => renderTemplate`<section class="card" data-astro-cid-szbgt4gh> <div class="season-header" data-astro-cid-szbgt4gh> <h2 class="card-title" data-astro-cid-szbgt4gh>🎵 Music &amp; Movies in the Park — ${yr} <span class="count-badge" data-astro-cid-szbgt4gh>${shells.length} events</span></h2> </div> <table class="shells-table" data-astro-cid-szbgt4gh> <thead data-astro-cid-szbgt4gh> <tr data-astro-cid-szbgt4gh> <th data-astro-cid-szbgt4gh>Date</th><th data-astro-cid-szbgt4gh>Start</th><th data-astro-cid-szbgt4gh>Venue</th><th data-astro-cid-szbgt4gh>Status</th> <th data-astro-cid-szbgt4gh>Band</th><th data-astro-cid-szbgt4gh>Confirmed</th><th data-astro-cid-szbgt4gh>Movie</th><th data-astro-cid-szbgt4gh>Actions</th> </tr> </thead> <tbody data-astro-cid-szbgt4gh> ${shells.map((shell) => renderTemplate`<tr${addAttribute(`shell-${shell.id}`, "id")} data-astro-cid-szbgt4gh> <td class="date-cell" data-astro-cid-szbgt4gh>${shell.displayDate}</td> <td data-astro-cid-szbgt4gh>${shell.startTime}</td> <td class="venue-cell" data-astro-cid-szbgt4gh>${shell.venue}</td> <td data-astro-cid-szbgt4gh><span${addAttribute(`badge badge-${shell.status}`, "class")} data-astro-cid-szbgt4gh>${shell.status}</span></td> <td data-astro-cid-szbgt4gh>${shell.bandName || renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>—</span>`}</td> <td data-astro-cid-szbgt4gh> ${shell.bandName ? shell.bandConfirmed ? renderTemplate`<span class="check-yes" data-astro-cid-szbgt4gh>✓ Yes</span>` : renderTemplate`<span class="check-no" data-astro-cid-szbgt4gh>✗ Pending</span>` : renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>—</span>`} </td> <td data-astro-cid-szbgt4gh>${shell.movie ? renderTemplate`<span class="check-yes" data-astro-cid-szbgt4gh>✓ ${shell.movie}</span>` : renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>—</span>`}</td> <td class="actions-cell" data-astro-cid-szbgt4gh> <button class="btn-sm btn-outline"${addAttribute(`openEditModal('${shell.id}','${(shell.venue || "").replace(/'/g, "\\'")}','${(shell.address || "").replace(/'/g, "\\'")}','${shell.startTime}','park_events','${(shell.imageUrl || "").replace(/'/g, "\\'")}','${(shell.display_type || "music").replace(/'/g, "\\'")}','${(shell.movie_title || "").replace(/'/g, "\\'")}','${(shell.movie_preview_embed || "").replace(/'/g, "\\'")}')`, "onclick")} data-astro-cid-szbgt4gh>
Edit
</button> ${shell.status === "band_assigned" && renderTemplate`<button class="btn-sm btn-confirm action-btn" data-action="confirm"${addAttribute(shell.id, "data-shell-id")}${addAttribute(shell.bandName, "data-band-name")} data-astro-cid-szbgt4gh>
Mark Confirmed
</button>`} ${(shell.status === "band_assigned" || shell.status === "confirmed") && renderTemplate`<button class="btn-sm btn-outline action-btn" data-action="unassign"${addAttribute(shell.id, "data-shell-id")}${addAttribute(shell.bandName, "data-band-name")} data-astro-cid-szbgt4gh>
Unassign
</button>`} ${shell.status !== "published" && renderTemplate`<button class="btn-sm btn-publish action-btn" data-action="publish"${addAttribute(shell.id, "data-shell-id")}${addAttribute(shell.bandName, "data-band-name")} data-astro-cid-szbgt4gh>
Publish ↗
</button>`} ${shell.status === "published" && renderTemplate`<button class="btn-sm btn-publish action-btn" data-action="publish"${addAttribute(shell.id, "data-shell-id")}${addAttribute(shell.bandName, "data-band-name")} data-republish="true" data-astro-cid-szbgt4gh>
Republish ↗
</button>`} ${shell.status === "shell" && renderTemplate`<form method="POST" style="display:inline" onsubmit="return confirm('Delete this event shell? This cannot be undone.')" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="delete_shell" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <input type="hidden" name="collection" value="park_events" data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-danger" data-astro-cid-szbgt4gh>Delete</button> </form>`} </td> </tr>`)} </tbody> </table> </section>`)} ${Object.entries(openMicBySeason).sort(([a], [b]) => Number(b) - Number(a)).map(([yr, shells]) => renderTemplate`<section class="card" data-astro-cid-szbgt4gh> <div class="season-header" data-astro-cid-szbgt4gh> <h2 class="card-title" data-astro-cid-szbgt4gh>🎤 Open Mic Night — ${yr} <span class="count-badge" data-astro-cid-szbgt4gh>${shells.length} events</span></h2> </div> <table class="shells-table" data-astro-cid-szbgt4gh> <thead data-astro-cid-szbgt4gh> <tr data-astro-cid-szbgt4gh> <th data-astro-cid-szbgt4gh>Date</th><th data-astro-cid-szbgt4gh>Start</th><th data-astro-cid-szbgt4gh>Venue</th><th data-astro-cid-szbgt4gh>Status</th><th data-astro-cid-szbgt4gh>Image</th><th data-astro-cid-szbgt4gh>Actions</th> </tr> </thead> <tbody data-astro-cid-szbgt4gh> ${shells.map((shell) => renderTemplate`<tr data-astro-cid-szbgt4gh> <td class="date-cell" data-astro-cid-szbgt4gh>${shell.displayDate}</td> <td data-astro-cid-szbgt4gh>${shell.startTime}</td> <td class="venue-cell" data-astro-cid-szbgt4gh>${shell.venue}</td> <td data-astro-cid-szbgt4gh><span${addAttribute(`badge badge-${shell.status}`, "class")} data-astro-cid-szbgt4gh>${shell.status}</span></td> <td data-astro-cid-szbgt4gh>${shell.imageUrl ? renderTemplate`<span class="check-yes" data-astro-cid-szbgt4gh>✓ Set</span>` : renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>—</span>`}</td> <td class="actions-cell" data-astro-cid-szbgt4gh> <button class="btn-sm btn-outline"${addAttribute(`openEditModal('${shell.id}','${(shell.venue || "").replace(/'/g, "\\'")}','${(shell.address || "").replace(/'/g, "\\'")}','${shell.startTime}','open_mic_events','${(shell.imageUrl || "").replace(/'/g, "\\'")}')`, "onclick")} data-astro-cid-szbgt4gh>
Edit
</button> ${shell.status !== "published" && renderTemplate`<button class="btn-sm btn-publish om-publish-btn"${addAttribute(shell.id, "data-shell-id")} data-astro-cid-szbgt4gh>
Publish ↗
</button>`} ${shell.status === "published" && renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "data-astro-cid-szbgt4gh": true }, { "default": async ($$result3) => renderTemplate` <span class="badge badge-published" data-astro-cid-szbgt4gh>Published</span> <button class="btn-sm btn-publish om-publish-btn"${addAttribute(shell.id, "data-shell-id")} data-republish="true" data-astro-cid-szbgt4gh>
Republish ↗
</button> ` })}`} ${shell.status === "shell" && renderTemplate`<form method="POST" style="display:inline" onsubmit="return confirm('Delete this event shell?')" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="delete_shell" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <input type="hidden" name="collection" value="open_mic_events" data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-danger" data-astro-cid-szbgt4gh>Delete</button> </form>`} </td> </tr>`)} </tbody> </table> </section>`)} ${Object.entries(festivalBySeason).sort(([a], [b]) => Number(b) - Number(a)).map(([yr, shells]) => shells.map((shell) => {
    const bands = shell.bands || [
      { slot: 1, time: "", bandId: null, bandName: null, confirmed: false },
      { slot: 2, time: "", bandId: null, bandName: null, confirmed: false },
      { slot: 3, time: "", bandId: null, bandName: null, confirmed: false }
    ];
    return renderTemplate`<section class="card fest-card"${addAttribute(`festival-${shell.id}`, "id")} data-astro-cid-szbgt4gh> <!-- Header row --> <div class="season-header" data-astro-cid-szbgt4gh> <div data-astro-cid-szbgt4gh> <h2 class="card-title" data-astro-cid-szbgt4gh>★ Magna Main Street Arts Festival — ${yr}</h2> <p class="fest-meta" data-astro-cid-szbgt4gh>${shell.displayDate} &middot; ${shell.startTime} &middot; ${shell.venue}</p> </div> <div class="fest-header-actions" data-astro-cid-szbgt4gh> <span${addAttribute(`badge badge-${shell.status}`, "class")} data-astro-cid-szbgt4gh>${shell.status}</span> <button class="btn-sm btn-outline"${addAttribute(`openEditModal('${shell.id}','${(shell.venue || "").replace(/'/g, "\\'")}',' ${(shell.address || "").replace(/'/g, "\\'")}',' ${shell.startTime}','festival_events')`, "onclick")} data-astro-cid-szbgt4gh>
Edit Details
</button> ${shell.status !== "published" && renderTemplate`<button${addAttribute(`btn-sm btn-publish festival-publish-btn${""}`, "class")}${addAttribute(shell.id, "data-shell-id")}${addAttribute(false, "disabled")}${addAttribute("Publish to public site", "title")} data-astro-cid-szbgt4gh>
Publish ↗
</button>`} ${shell.status === "published" && renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "data-astro-cid-szbgt4gh": true }, { "default": async ($$result3) => renderTemplate` <span class="badge badge-published" data-astro-cid-szbgt4gh>✓ Published</span> <button class="btn-sm btn-outline festival-publish-btn"${addAttribute(shell.id, "data-shell-id")} data-republish="true" title="Update the public festival page with latest bands and info" data-astro-cid-szbgt4gh>
Republish ↗
</button> ` })}`} ${shell.status === "shell" && renderTemplate`<form method="POST" style="display:inline" onsubmit="return confirm('Delete this festival shell?')" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="delete_shell" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <input type="hidden" name="collection" value="festival_events" data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-danger" data-astro-cid-szbgt4gh>Delete</button> </form>`} </div> </div> <!-- Description preview --> ${shell.description && renderTemplate`<p class="fest-description" data-astro-cid-szbgt4gh>${shell.description}</p>`} <div class="fest-body" data-astro-cid-szbgt4gh> <!-- ── MAIN STAGE BANDS ── --> <div class="fest-section" data-astro-cid-szbgt4gh> <h3 class="fest-section-heading" data-astro-cid-szbgt4gh>🎸 Main Stage Bands</h3> <table class="shells-table" data-astro-cid-szbgt4gh> <thead data-astro-cid-szbgt4gh> <tr data-astro-cid-szbgt4gh> <th data-astro-cid-szbgt4gh>Slot</th><th data-astro-cid-szbgt4gh>Time</th><th data-astro-cid-szbgt4gh>Band</th><th data-astro-cid-szbgt4gh>Status</th><th data-astro-cid-szbgt4gh>Actions</th> </tr> </thead> <tbody data-astro-cid-szbgt4gh> ${bands.map((b) => renderTemplate`<tr data-astro-cid-szbgt4gh> <td class="slot-cell" data-astro-cid-szbgt4gh>Slot ${b.slot}</td> <td class="time-cell" data-astro-cid-szbgt4gh>${b.time || renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>—</span>`}</td> <td class="band-cell" data-astro-cid-szbgt4gh>${b.bandName || renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>Unassigned</span>`}</td> <td data-astro-cid-szbgt4gh> ${b.bandId ? b.confirmed ? renderTemplate`<span class="check-yes" data-astro-cid-szbgt4gh>✓ Confirmed</span>` : renderTemplate`<span class="check-no" data-astro-cid-szbgt4gh>✗ Pending</span>` : renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>—</span>`} </td> <td class="actions-cell" data-astro-cid-szbgt4gh> ${!b.bandId && renderTemplate`<button class="btn-sm btn-assign-fest"${addAttribute(shell.id, "data-shell-id")}${addAttribute(b.slot, "data-slot")} data-astro-cid-szbgt4gh>
Assign Band
</button>`} ${b.bandId && !b.confirmed && renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "data-astro-cid-szbgt4gh": true }, { "default": async ($$result3) => renderTemplate` <form method="POST" style="display:inline" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="confirm_festival_band" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <input type="hidden" name="slot"${addAttribute(b.slot, "value")} data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-confirm" data-astro-cid-szbgt4gh>Mark Confirmed</button> </form> <form method="POST" style="display:inline" onsubmit="return confirm('Remove this band from the slot?')" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="unassign_festival_band" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <input type="hidden" name="slot"${addAttribute(b.slot, "value")} data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-outline" data-astro-cid-szbgt4gh>Unassign</button> </form> ` })}`} ${b.bandId && b.confirmed && renderTemplate`<form method="POST" style="display:inline" onsubmit="return confirm('Remove confirmed band from slot?')" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="unassign_festival_band" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <input type="hidden" name="slot"${addAttribute(b.slot, "value")} data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-outline" data-astro-cid-szbgt4gh>Unassign</button> </form>`} </td> </tr>`)} </tbody> </table> </div> <!-- ── VENDORS SUMMARY ── --> <div class="fest-section" data-astro-cid-szbgt4gh> <h3 class="fest-section-heading" data-astro-cid-szbgt4gh>🛒 Vendors</h3> <div class="vendor-summary-card" data-astro-cid-szbgt4gh> <div class="vendor-summary-stat" data-astro-cid-szbgt4gh> <span class="vendor-stat-number" data-astro-cid-szbgt4gh>${approvedVendorCount}</span> <span class="vendor-stat-label" data-astro-cid-szbgt4gh>Approved &amp; Paid</span> </div> <div class="vendor-summary-actions" data-astro-cid-szbgt4gh> <a href="/admin/vendors" class="btn btn-outline btn" data-astro-cid-szbgt4gh>Manage Vendors ↗</a> <a href="/admin/vendors?filter=approved" class="btn-sm btn-outline" data-astro-cid-szbgt4gh>Pending Assignment</a> </div> </div> <p class="vendor-hint" data-astro-cid-szbgt4gh>Booth space numbers are assigned on the Vendors page. Approved vendors automatically appear on the public festival page.</p> </div> </div> </section>`;
  }))} ${Object.entries(artNightBySeason).sort(([a], [b]) => Number(b) - Number(a)).map(([yr, shells]) => renderTemplate`<section class="card" data-astro-cid-szbgt4gh> <div class="season-header" data-astro-cid-szbgt4gh> <h2 class="card-title" data-astro-cid-szbgt4gh>✦ Group Art Night — ${yr} <span class="count-badge" data-astro-cid-szbgt4gh>${shells.length} events</span></h2> </div> <table class="shells-table" data-astro-cid-szbgt4gh> <thead data-astro-cid-szbgt4gh> <tr data-astro-cid-szbgt4gh> <th data-astro-cid-szbgt4gh>Date</th><th data-astro-cid-szbgt4gh>Start</th><th data-astro-cid-szbgt4gh>Venue</th><th data-astro-cid-szbgt4gh>Status</th><th data-astro-cid-szbgt4gh>Presenter</th><th data-astro-cid-szbgt4gh>Actions</th> </tr> </thead> <tbody data-astro-cid-szbgt4gh> ${shells.map((shell) => renderTemplate`<tr data-astro-cid-szbgt4gh> <td class="date-cell" data-astro-cid-szbgt4gh>${shell.displayDate}</td> <td data-astro-cid-szbgt4gh>${shell.startTime}</td> <td class="venue-cell" data-astro-cid-szbgt4gh>${shell.venue}</td> <td data-astro-cid-szbgt4gh><span${addAttribute(`badge badge-${shell.status}`, "class")} data-astro-cid-szbgt4gh>${shell.status}</span></td> <td data-astro-cid-szbgt4gh>${shell.presenterName || renderTemplate`<span class="dim" data-astro-cid-szbgt4gh>—</span>`}</td> <td class="actions-cell" data-astro-cid-szbgt4gh> <button class="btn-sm btn-outline"${addAttribute(`openEditModal('${shell.id}','${(shell.venue || "").replace(/'/g, "\\'")}','${(shell.address || "").replace(/'/g, "\\'")}','${shell.startTime}','art_night_events','${(shell.imageUrl || "").replace(/'/g, "\\'")}')`, "onclick")} data-astro-cid-szbgt4gh>
Edit
</button> ${!shell.presenterId && renderTemplate`<button class="btn-sm btn-assign-presenter"${addAttribute(shell.id, "data-shell-id")} data-astro-cid-szbgt4gh>
Assign Presenter
</button>`} ${shell.presenterId && !shell.presenterConfirmed && renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "data-astro-cid-szbgt4gh": true }, { "default": async ($$result3) => renderTemplate` <form method="POST" style="display:inline" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="confirm_presenter" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-confirm" data-astro-cid-szbgt4gh>Mark Confirmed</button> </form> <form method="POST" style="display:inline" onsubmit="return confirm('Remove this presenter?')" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="unassign_presenter" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-outline" data-astro-cid-szbgt4gh>Unassign</button> </form> ` })}`} ${shell.status !== "published" && renderTemplate`<button class="btn-sm btn-publish an-publish-btn"${addAttribute(shell.id, "data-shell-id")} data-astro-cid-szbgt4gh>
Publish ↗
</button>`} ${shell.status === "published" && renderTemplate`<button class="btn-sm btn-publish an-publish-btn"${addAttribute(shell.id, "data-shell-id")} data-republish="true" data-astro-cid-szbgt4gh>
Republish ↗
</button>`} ${shell.status === "shell" && renderTemplate`<form method="POST" style="display:inline" onsubmit="return confirm('Delete this event shell?')" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="delete_shell" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId"${addAttribute(shell.id, "value")} data-astro-cid-szbgt4gh> <input type="hidden" name="collection" value="art_night_events" data-astro-cid-szbgt4gh> <button type="submit" class="btn-sm btn-danger" data-astro-cid-szbgt4gh>Delete</button> </form>`} </td> </tr>`)} </tbody> </table> </section>`)}${totalShells === 0 && renderTemplate`<div class="empty-state" data-astro-cid-szbgt4gh> <p data-astro-cid-szbgt4gh>No event shells yet. Select an event type above to get started.</p> </div>`} <div id="presenter-modal" class="modal-overlay" hidden data-astro-cid-szbgt4gh> <div class="modal-box" data-astro-cid-szbgt4gh> <h3 data-astro-cid-szbgt4gh>✦ Assign Presenter</h3> <form method="POST" id="presenter-form" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="assign_presenter" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId" id="presenter-modal-shell-id" data-astro-cid-szbgt4gh> <input type="hidden" name="presenterId" id="presenter-modal-presenter-id" data-astro-cid-szbgt4gh> <input type="hidden" name="presenterName" id="presenter-modal-presenter-name" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label data-astro-cid-szbgt4gh>Select from Art Class Submissions</label> <div id="presenter-list" class="fest-band-list" data-astro-cid-szbgt4gh></div> </div> <div class="modal-actions" data-astro-cid-szbgt4gh> <button type="button" class="btn btn-outline" id="presenter-modal-cancel" data-astro-cid-szbgt4gh>Cancel</button> <button type="submit" class="btn btn-primary" id="presenter-modal-submit" disabled data-astro-cid-szbgt4gh>Assign Presenter →</button> </div> </form> </div> </div>  <div id="fest-band-modal" class="modal-overlay" hidden data-astro-cid-szbgt4gh> <div class="modal-box" data-astro-cid-szbgt4gh> <h3 data-astro-cid-szbgt4gh>🎸 Assign Band — Slot <span id="fest-modal-slot-label" data-astro-cid-szbgt4gh></span></h3> <form method="POST" id="fest-band-form" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="assign_festival_band" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId" id="fest-modal-shell-id" data-astro-cid-szbgt4gh> <input type="hidden" name="slot" id="fest-modal-slot" data-astro-cid-szbgt4gh> <input type="hidden" name="bandId" id="fest-modal-band-id" data-astro-cid-szbgt4gh> <input type="hidden" name="bandName" id="fest-modal-band-name" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="fest-modal-time" data-astro-cid-szbgt4gh>Set Time <span class="field-hint" data-astro-cid-szbgt4gh>e.g. 12:00 PM</span></label> <input type="text" id="fest-modal-time" name="slotTime" placeholder="12:00 PM" required data-astro-cid-szbgt4gh> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label data-astro-cid-szbgt4gh>Select Band</label> <div id="fest-band-list" class="fest-band-list" data-astro-cid-szbgt4gh></div> </div> <div class="modal-actions" data-astro-cid-szbgt4gh> <button type="button" class="btn btn-outline" id="fest-modal-cancel" data-astro-cid-szbgt4gh>Cancel</button> <button type="submit" class="btn btn-primary" id="fest-modal-submit" disabled data-astro-cid-szbgt4gh>Assign Band →</button> </div> </form> </div> </div>  <div id="edit-modal" class="modal-overlay" hidden data-astro-cid-szbgt4gh> <div class="modal-box" data-astro-cid-szbgt4gh> <h3 data-astro-cid-szbgt4gh>Edit Event Shell</h3> <form method="POST" id="edit-form" data-astro-cid-szbgt4gh> <input type="hidden" name="action" value="update_shell" data-astro-cid-szbgt4gh> <input type="hidden" name="shellId" id="edit-shell-id" data-astro-cid-szbgt4gh> <input type="hidden" name="collection" id="edit-collection" value="park_events" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="edit-venue" data-astro-cid-szbgt4gh>Venue</label> <input type="text" id="edit-venue" name="venue" required data-astro-cid-szbgt4gh> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="edit-address" data-astro-cid-szbgt4gh>Address</label> <input type="text" id="edit-address" name="address" required data-astro-cid-szbgt4gh> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="edit-startTime" data-astro-cid-szbgt4gh>Start Time</label> <input type="text" id="edit-startTime" name="startTime" required data-astro-cid-szbgt4gh> </div> <!-- Movie fields — only shown for park_events --> <div id="edit-movie-section" style="display:none" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="edit-displayType" data-astro-cid-szbgt4gh>Display Type</label> <select id="edit-displayType" name="displayType" data-astro-cid-szbgt4gh> <option value="music" data-astro-cid-szbgt4gh>Music only</option> <option value="movie" data-astro-cid-szbgt4gh>Movie only</option> <option value="both" data-astro-cid-szbgt4gh>Music &amp; Movie</option> </select> </div> <div id="edit-movie-fields" style="display:none; flex-direction:column; gap:12px; margin-top:12px" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label for="edit-movieTitle" data-astro-cid-szbgt4gh>Movie Title</label> <input type="text" id="edit-movieTitle" name="movieTitle" placeholder="e.g. The Princess Bride" data-astro-cid-szbgt4gh> </div> <div class="form-field" data-astro-cid-szbgt4gh> <label for="edit-moviePreviewEmbed" data-astro-cid-szbgt4gh>Movie Preview Embed <span class="field-hint" data-astro-cid-szbgt4gh>YouTube embed src</span></label> <textarea id="edit-moviePreviewEmbed" name="moviePreviewEmbed" rows="3" placeholder="https://www.youtube.com/embed/..." data-astro-cid-szbgt4gh></textarea> </div> </div> </div> <!-- Image replace — only shown for event types that have images --> <div id="edit-image-section" style="display:none" data-astro-cid-szbgt4gh> <div class="form-field" data-astro-cid-szbgt4gh> <label data-astro-cid-szbgt4gh>Replace Image <span class="field-hint" data-astro-cid-szbgt4gh>Leave blank to keep current image</span></label> <!-- Populated by JS after upload --> <input type="hidden" id="edit-imageUrl" name="imageUrl" data-astro-cid-szbgt4gh> <div id="edit-current-image" style="display:none; margin-bottom:8px" data-astro-cid-szbgt4gh> <img id="edit-current-img-el" src="" alt="Current image" style="width:100%; max-height:140px; object-fit:cover; border-radius:6px; border:1.5px solid var(--border)" data-astro-cid-szbgt4gh> <p style="font-size:11px; color:var(--muted); margin:4px 0 0" data-astro-cid-szbgt4gh>Current image</p> </div> <div class="upload-zone" id="edit-image-zone" data-astro-cid-szbgt4gh> <input type="file" id="edit-image-input" accept="image/jpeg,image/png,image/webp" class="upload-input" data-astro-cid-szbgt4gh> <label for="edit-image-input" class="upload-label" data-astro-cid-szbgt4gh> <span class="upload-icon" data-astro-cid-szbgt4gh>🖼️</span> <span class="upload-text" data-astro-cid-szbgt4gh><strong data-astro-cid-szbgt4gh>Choose a replacement</strong> or drag and drop</span> <span class="upload-hint" data-astro-cid-szbgt4gh>JPG, PNG or WebP · max 10 MB</span> </label> <div class="upload-preview" id="edit-image-preview" hidden data-astro-cid-szbgt4gh></div> </div> <div id="edit-upload-status" class="upload-status" hidden data-astro-cid-szbgt4gh></div> </div> </div> <div class="modal-actions" data-astro-cid-szbgt4gh> <button type="button" class="btn btn-outline" id="modal-cancel-btn" data-astro-cid-szbgt4gh>Cancel</button> <button type="submit" class="btn btn-primary" id="edit-save-btn" data-astro-cid-szbgt4gh>Save Changes</button> </div> </form> </div> </div> ` }), defineScriptVars({ firebaseConfig, approvedBands, artClassSubmissions }));
}, "/Users/wfurgason/Documents/Arts Council/magnaarts.org/src/pages/admin/planning.astro", void 0);
const $$file = "/Users/wfurgason/Documents/Arts Council/magnaarts.org/src/pages/admin/planning.astro";
const $$url = "/admin/planning";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Planning,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
